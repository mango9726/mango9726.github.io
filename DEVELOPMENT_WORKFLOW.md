# DEVELOPMENT_WORKFLOW.md

> **Persistent Developer Workflow & Task Tracker** for the Vocab Trainer web app.
> Claude reads this file at the start of every coding session and updates the
> **Dynamic Task Board** immediately after any change. Keep it clean and current.

---

## 1. Project Overview

**What it is**
A browser-based **English vocabulary trainer** (B1 level, with a Thai-language UI) called
*Vocab Trainer*. It helps you learn words through spaced repetition (Leitner/SRS) plus a
suite of study modes and mini-games: Flashcards, Quiz, Pronunciation (mic), Fill-in-the-Blank,
Card Match, True/False (timed), Hangman, Sentence Builder, Cloze, and Listen & Type. It also
has Daily Tasks, a Browse/Search view, theme + sound + music settings, and a backup/restore
system. Most recently, an in-game **Spotify-style Mini Music Player** overlay was added.

**Tech stack**
- **Vanilla HTML5 + CSS3 + JavaScript** (ES5/ES6). **No framework, no bundler, no transpile.**
- **No backend, no database.** All progress lives in the browser via `localStorage`.
- **Audio:** HTML5 `<audio>` for music + the Web Audio API for UI sound effects (no files needed).
- **Icons:** inline SVG icon set (no icon library).
- **Fonts:** Google Fonts — *Inter* + *Noto Sans Thai*.
- **Served as a static site** by Python's `http.server` (no Node.js in the toolchain).
- **No package manager, no build step**; tests run with plain Node (`node --test "tests/*.test.js"`, see `web/vocab/package.json`).

**Project structure**
```
codex/                         ← repository root
├─ CLAUDE.md                   ← project memory / standing instructions
├─ README.md                   ← original project readme
├─ WORKFLOW.md                 ← knowledge-base day-to-day routine
├─ notes/                      ← markdown knowledge base
├─ web/vocab/                  ← ★ THE WEB APP (this is what you run) ★
│  ├─ index.html               ← entry point (loads the scripts below, in order)
│  ├─ vocab-data.js            ← word/collocation/idiom data
│  ├─ app.js                   ← all UI + game logic (one big IIFE)
│  ├─ style.css                ← app styles (uses --accent/--panel/--text tokens, light+dark)
│  ├─ mini-player.js           ← Spotify-style music overlay (manager + UI)
│  ├─ mini-player.css          ← music overlay styles (clean white & blue, decoupled from host theme)
│  ├─ MINI_PLAYER_GUIDE.md     ← how to integrate / hook up the player
│  ├─ start-server.bat         ← Windows launcher (python -m http.server 8000)
│  ├─ button sound.mp3         ← UI click sound
│  └─ song/
│     ├─ onpage/               ← background "on-page" music (10 tracks)
│     └─ ingame/               ← "in-game" music (8 tracks)
└─ .claude/                    ← Claude Code session settings
```

**Main entry point:** `web/vocab/index.html` → `vocab-data.js` → `app.js` → `mini-player.js`.
The app is a single page; "views" (Home, Games, Settings, …) are sections toggled by JS, not
separate routes.

---

## 2. Quick Start Commands

> ⚠️ **Always open the site over `http://`, never `file://`.** The microphone prompt,
> audio autoplay, and reliable `localStorage` all require a real origin. Double-clicking
> `index.html` will mostly work but the mic prompt re-appears every time.

**Prerequisites**
- Python 3.x (this machine has **3.14**). No Node.js required.
- A modern browser — **Chrome or Edge** recommended (mic + autoplay behave best).

**Run the dev server**
```bash
# Windows (double-click also works):
cd web/vocab
start-server.bat
# → serves http://localhost:8000

# Any OS / manual:
cd web/vocab
python -m http.server 8000
# → open http://localhost:8000 in your browser
```

**Build** — none. It's static; what you see in `web/vocab/` is what ships.

**Test** — `npm test` (from `web/vocab/`) runs `node --test "tests/*.test.js"` — pure Node unit tests for FSRS-5 (`tests/fsrs.test.js`), CSV parsing/import (`tests/csv.test.js`), and TH↔EN i18n key parity (`tests/i18n.test.js`). No browser tests yet.

**Deploy** — copy the `web/vocab/` folder to any static host (GitHub Pages, Netlify,
Cloudflare Pages, an S3 bucket, etc.). No build step. If you move it to a sub-path,
update asset/script paths accordingly.

**Lint / format** — none configured.

---

## 3. Daily Warm-up Checklist

Do this every time you (or Claude) start work on the project:

- [ ] **1. Sync & check git.** At the repo root: `git status`, then `git pull`
      (current branch is `master`; PRs target `main`).
- [ ] **2. Verify tooling.** `python --version` (needs 3.x). No `npm install` needed.
- [ ] **3. Start the server.** `cd web/vocab && start-server.bat`
      (or `python -m http.server 8000`).
- [ ] **4. Open it.** Browse to `http://localhost:8000` in Chrome/Edge; allow the
      microphone once when prompted.
- [ ] **5. Read the state.** *(Claude does this automatically.)* You: skim the
      **Dynamic Task Board** below to see what's next.
- [ ] **6. Smoke test.** Nav works, a study mode loads, and the **Music** tab
      (bottom-right) expands and plays a track.
- [ ] **7. Pick a task.** Start the top **In Progress** item, or pull the next
      **Backlog** item. Mark it `[/]` before you begin.

---

## 4. Dynamic Task Board

> Legend: `[ ]` Backlog · `[/]` In Progress · `[x]` Done (stamped YYYY-MM-DD).
> Keep tasks small and move them the moment their status changes.

### Backlog (upcoming features / fixes / tech debt)
- [ ] *(add new ideas here as they come up)*
- [x] **Modern auth system UI** — redesigned login/register modal with glassmorphism gradient hero, social login buttons (Google/GitHub/Apple), password visibility toggle, password strength indicator, loading spinner, forgot password flow, terms/privacy/help links, modern profile modal with stats cards, improved sidebar auth button — 2026-08-06
- [x] **Fix user data isolation** — backend data now syncs to SecureStore on login/verify; user data cleared from SecureStore on logout; each user only sees their own data — 2026-08-06
- [x] **Added CEFR-level vocabulary from Oxford** — A1, A2, B1, B2, C1, C2 levels with vocabulary, collocations, and idioms across 9 new days in vocab-data.js — 2026-08-08
- [x] **Added richer per-word data** — synonyms/antonyms, example audio, topic tags (feeds mnemonics + SRS) for all vocabulary items — 2026-08-08

### In Progress (current focus)
- [ ] *(empty — pick up from the Backlog items above.)*

### Done (recently completed)
- [x] **Fix Login UI Crash (ReferenceError in auth.js)** — fixed a ReferenceError caused by referencing `signInWithGithub` and `signInWithApple` in `auth.js`'s export map without them being defined. This error had crashed `auth.js` on startup and prevented the login modal from opening altogether when clicking the login button. Verified: `node --check` clean, **26/26 tests pass** — 2026-09-02
- [x] **Graded reader UI polish: removed static difficulty badge and upgraded hover difficulty display** — removed the static A2/level badge from story cards (`sotdCardHtml` and `renderStories`), and added a sleek, animated hover difficulty badge (`.story-hover-badge`) showing the level and localized level name (with signature CEFR level color) when hovering the mouse over any story card in the Graded Reader view. Verified: `node --check` clean, **26/26 tests pass** — 2026-08-24
- [x] **AI-Generated Custom Stories via Backend Proxy (`/api/ai/generate-story`)** — integrated Gemini AI API securely using a local `.env` configuration on the backend server (`server.js`), pulling the user's weak words (`getWeakWords()`) and generating customized English+Thai stories displayed instantly in the story reader. Verified: `node --check` clean, **26/26 tests pass** — 2026-08-23
- [x] **Expanded B1–C2 CEFR levels from 60 to 90 days each (480 days total, 4,800 unique words)** — updated `cefr.js` day ranges (A1–A2: 1–120, B1–C2: 121–480 at 90 days / 900 words per level), `vocab-data.js` level configurations and word pool targets (900 words per level for B1–C2), and `app.js` max day limits (480 total, 90 per level for game sources). Verified: `node --check` clean, **26/26 tests pass** — 2026-08-22
- [x] **Sentence Pronunciation Practice (พูดประโยคตัวอย่าง)** — added a "Speak Example Sentence" button inside the word detail modal (`detailSentencePron`) powered by `attachMic`, allowing users to test their pronunciation and accuracy on full example sentences (`item.exEn`) with syllable-level feedback and bilingual i18n support. Verified: `node --check` clean, **26/26 tests pass** — 2026-08-21
- [x] **Expanded placement test question bank to 180 questions (30 per CEFR level)** — added 60 new well-calibrated questions (10 per level, A1 to C2) to `placement.js` complete with difficulty parameters, discrimination, guessing factor, and categories, improving adaptive IRT placement accuracy and reducing question repetition. Verified: `node --check` clean, **26/26 tests pass** — 2026-08-21
- [x] **Stories & articles made natural / everyday — 300 generated articles + all 59 curated stories (EN+TH, A1–C2)** — user asked to make sentences/stories feel like real everyday life. Two parts: (1) **300 auto-generated articles** (`getArticleText`/`getArticleThai` in `app.js`) were stiff AI-filler templates ("This is a short story about X. The sun shines brightly…"). Rewrote all 6 CEFR template pairs (A1–C2) into a semi-formal, daily-life tone ("People talk about X more often than we might think…") with the topic woven in as an **object** (not subject) so verb agreement stays correct for both noun-phrase and gerund titles ("Playing with Toys", "The Benefits of Traveling"); (2) **all 59 curated stories** (`CURATED_STORIES`, fairy/ghost/adventure/scifi/mystery/classic) got a naturalization pass — contractions where people really use them ("You're so slow!" / "Let's have a race, then." / "She didn't answer."), less textbook stiffness ("pulled over and offered her a ride"), Thai rewritten to read natively, and several **Thai typos fixed** (e.g. กระต่าย "领先มาก"→"นำหน้าไปไกล", "หม่าป่า"→"หมาป่า", Goldilocks "ก cried"→"ร้องขึ้น"). Classics (Gatsby, Poe, Frankenstein, Moby Dick…) kept appropriately serious; fairy tales warmer. Applied via 4 parallel content agents writing JSON → scripted replacement in `app.js`. Verified: `node --check` clean, **26/26 tests pass**, app.js + style.css braces balanced, all 59 stories page-count EN=TH, 0 stray English in Thai text, headless-Chrome CDP: Stories view renders 359 cards (300+59), opening "The Time Machine" shows the new phrasing (no stale text) — 2026-08-21
- [x] **Placement test: options 5–6 are now real Thai meaning choices (no more "คำตอบอื่น"/"ไม่ใช่ความหมายนี้")** — every question in `placement.js` had 6 options where the last two were meta-options ("คำตอบอื่น" = other answer, "ไม่ใช่ความหมายนี้" = not this meaning) that aren't word translations. Replaced both on **all 120 questions** (20 per CEFR level) with two real, level-appropriate Thai translations drawn from the same bank: preferred correct meanings of other same-level same-category words (e.g. for A1 "water": แม่, โรงเรียน), then same-level any-category, then other same-level distractor strings — never duplicating a question's own 4 options or correct answer. Options render in fixed order (A–F) with `answer` index 0–3, so `answer` stayed valid (verified: 0 out-of-range, 0 duplicate options, 0 leftover meta strings). Verified: `node --check` clean, **26/26 tests pass** — 2026-08-20
- [x] **"Mark today as done" (เรียนเสร็จแล้ว) in Daily Tasks + studied words flow into the review cycle** — user wanted a button to say "I've finished studying today" and for the day's words to then enter review/other systems. Added a dedicated "Mark today as done" card at the top of the Daily Tasks list (`renderTasks`): (1) **button** `markDayDone()` stores `settings.dayDone[planDay] = today` (persisted via SecureStore), seeds today's words into the FSRS review pool (`progress[id].seen++` + `due = tomorrow`, only for words not yet seen), calls `recordReview(planDay)` so the day returns on the GAPS schedule (days 2, 4, 7, 14…), `bumpStreak()` for the day, shows a "Done! {n} words in your review cycle" toast, and flips the card to a green completed state with an **Undo** button (`unmarkDayDone()`); (2) **auto-mark** — finishing the new-word quiz for the current plan day now also marks the day done automatically; (3) **i18n** — 11 new TH+EN keys (`tasks.markDone`, `tasks.markDoneHint`, `tasks.markDoneMeta`, `tasks.markDoneBtn`, `tasks.doneBadge`, `tasks.doneTitle`, `tasks.doneMeta`, `tasks.undoDone`, `tasks.doneToast`, `tasks.unmarkedToast`, `tasks.noneToMark`) with full TH↔EN parity; (4) **CSS** — `.task-done-card` (+`.done` green state, plain `.btn` for Undo since no `.btn-ghost` exists). Words marked done now show up in the Due forecast, Due-only quiz filter, Boss Rush weak-words, and the Daily Tasks review loop on later days. Verified: `node --check` clean on `app.js`/`vocab-i18n.js`, **26/26 tests pass**, CSS braces balanced, headless-Chrome CDP test: card renders → click sets `dayDone:{"20":"2026-08-20"}` + seeds 11 words into progress → undo clears it — 2026-08-20
- [x] **Responsive layout audit + fixes — zero horizontal overflow at every size (320–1920px)** — built CDP-based audit harness (`web/vocab/_audit.html` + `assets/js/_audit.js` + headless-Chrome runner) testing all 18 views at 24 widths; found the page already had a comprehensive ≤1024px responsive layer (drawer sidebar, fluid grids, stacked hero) and zero page-level overflow, then fixed the real remaining issues: (1) **Stories grid overflowed 14px at 320px** — `#storiesList` inline `minmax(280px,1fr)` exceeded the 266px content width → changed to `minmax(min(280px,100%),1fr)` in `index.html` (+ `_audit.html`/`_resptest.html` copies); (2) **hero side column squeezed the greeting at 641–900px** — added a ≤900px block stacking `.hero` to a column and spreading the streak/WOTD cards; (3) **browse/quiz control fields couldn't shrink** — `.controls` got `width:100%`, `.field` got `min-width:0`, chip-groups `max-width:100%`; (4) **global safety net** — `html, body { overflow-x: clip }` so nothing can ever cause page-level horizontal scroll; (5) story cards wrap their header/meta rows on phones (defensive). Verified with the CDP audit: **0 page-overflow entries at all 24 widths**; remaining detector hits are proven false positives (SVG chart `<text>`, the fixed Games dropdown, and a transient early-init render race that never occurs in settled real-page use). Also confirmed the earlier `getP()` harness error was a harness artifact (calls `showView` before async init completes), not a user-facing bug — 2026-08-20
- [x] **Google sign-in "didn't finish" toast: fix false positives + stale flag** — user reported the `auth.googleRedirectEmpty` toast ("Google sign-in didn't finish — try again…") appearing. Deployed v5 code was confirmed live (SW + auth.js match working tree), so the message was a false positive or stale-marker artifact rather than missing code. Two fixes in `auth.js`: (1) **race guard** — the redirect-empty branch now waits 1.5s (after `onAuthStateChanged` has had a chance to confirm a fresh session) and checks `getUser()` again before showing the toast, so a login that *just* succeeded no longer triggers the error; (2) **stale-marker guard** — `vocab_oauth_pending` now stores a JSON `{t: timestamp}`, and the branch only treats it as a real failed redirect if set within the last 60s; an old marker left over from an earlier interrupted attempt is silently cleared instead of toasting on every page load. Also bumped SW `VERSION` → `vocab-trainer-v6` so the updated `auth.js` actually reaches clients (SW is cache-first for static assets). Verified: `node --check` clean on `auth.js`/`service-worker.js`, **26/26 tests pass** — 2026-08-17
- [x] **Google login on GitHub Pages: make the auth flow self-diagnosing + fix stale service worker** — user still saw "กลับมาหน้าเดิมเหมือนยังไม่ได้ล็อกอิน, ปุ่มล็อกอินไม่หาย" even after the earlier error-toast fix, and confirmed they had already added the domain to Firebase Authorized domains and saw **no toast**. Two gaps found: (1) **stale SW was serving old code** — `service-worker.js` used **cache-first** for static assets but was still at `VERSION="vocab-trainer-v4"` and **`auth.js`/`firebase-config.js` were NOT in the SHELL precache**, so users kept running the pre-fix bundle (silent `console.warn`, no toast) no matter how many redeploys happened; bumped `VERSION`→`vocab-trainer-v5` + added both files to SHELL so new code actually reaches clients; (2) **`onAuthStateChanged` was registered only *after* `getRedirectResult()` resolved** (chained in `.then()`), so if that call hung or returned `null` the app never picked up a persisted Firebase session → logged out forever with no message; also, a **silent `null` redirect result** (vs. a thrown error) produced zero user feedback. Fixes in `auth.js`: register `onAuthStateChanged` **immediately and unconditionally** in `initAuthUI()` (persisted session always detected), add a `vocab_oauth_pending` sessionStorage marker set in `signInWithGoogle()` before `signInWithRedirect()` (cleared on success/throw/`getRedirectResult` error) so that when a redirect returns with **no session** the app shows a clear toast `auth.googleRedirectEmpty` (TH+EN: "ล็อกอินด้วย Google ยังไม่เสร็จสมบูรณ์ — ลองอีกครั้ง / ตรวจสอบ Firebase Authorized domains") instead of failing silently. Verified: `node --check` clean on `auth.js`/`vocab-i18n.js`/`service-worker.js`, **26/26 tests pass**, headless-Chrome harness (via `cmd /c` output redirect — Edge/Chrome `--dump-dom` prints nothing through PowerShell on this machine): `redirect` **PASS** (getRedirectResult → sidebar "T TestGoogle" + logged-in), `observer` **PASS** (onAuthStateChanged path), `reject` toast check **PASS** (the harness's trailing login-wait timeout on `reject` is a harness artifact — that scenario must not log in, so it always times out by design) — 2026-08-17
- [x] **Remove user-facing algorithm jargon across the web app** — user asked to strip technical terms regular users don't need (e.g. "Adaptive IRT v3 (3PL)", "Response-Time Scoring", "Stability", "Avg retention", "Spaced Review"). Swept all user-facing text and simplified: (1) **placement.js intro modal** — removed the marketing claim "แม่นยำกว่าและเร็วกว่าแบบทดสอบแบบเดิม", replaced chip "Adaptive IRT v3 (3PL)" → "ปรับความยากอัตโนมัติ" and "Response-Time Scoring" → "คำนึงถึงเวลาที่ใช้ตอบ", reworded the intro line "ตอบคำถามแบบ Adaptive (ไม่เกิน 35 ข้อ)" → "ตอบคำถามไม่เกิน 35 ข้อ"; (2) **app.js word detail modal** — "Memory strength <b>X%</b>" → "You remember <b>X%</b>", "Stability: <b>Yd</b>" → "Memorized: <b>Yd</b>"; (3) **vocab-i18n.js** — EN "Avg retention" → "Avg recall", "Total lapses" → "Times forgotten", "Daily Tasks — Spaced Review" → "Daily Tasks — Smart Review", "Memory Strength" → "Memory", plain-English memory hint, reworded `tasks.hint` (TH: "ในช่วงห่างที่เพิ่มขึ้น" → "เป็นระยะ — ยิ่งจำได้ดี คำยิ่งกลับมานานขึ้น"; EN: "at increasing intervals" → "the better you remember them, the longer until they return"); (4) **index.html** fallback texts synced to the cleaned EN strings. Left untouched: internal code comments/identifiers, raw CSV export column names (machine-readable data format), and test-harness copies. Verified: `node --check` clean on `app.js`/`placement.js`/`vocab-i18n.js`, **26/26 tests pass** — 2026-08-17
- [x] **Google login verified + redirect failures now show an error toast** — user reported "ล็อกอินด้วยกูเกิลแล้วไม่ขึ้นโปรไฟล์". Diagnosed with a new headless harness `web/vocab/_googletest.html` (+ `_googletest-seed.js` / `_googletest-runner.js`, a same-origin copy of index.html with the 3 gstatic Firebase SDK `<script>` tags stripped and a mocked `window.firebase` injected before `firebase-config.js`; MutationObserver captures toasts the instant they append). Findings: (1) **the login code path is correct** — with the mock, `firebaseCheckRedirectResult()` → `getRedirectResult` → `setToken`/`setUser` → `updateSidebarAuthBtn()` turns the sidebar button into the Google profile and the profile modal opens; the `onAuthStateChanged` fallback path works too (token/user verified round-tripping through SecureStore's `v1:` ciphertext). So a "no profile after Google login" symptom in the real app is environmental — almost always the Firebase project's **Authorized domains** list (`auth/unauthorized-domain`), which previously failed **silently** (only a `console.warn`); (2) surfaced that failure: `firebaseCheckRedirectResult()`'s catch now shows `firebaseErrorMessage(e)` as an error toast (e.g. the Thai "Domain นี้ยังไม่ได้เพิ่มใน Firebase Authorized domains…" message) instead of staying silent on "Login"; (3) while testing this, found + fixed a real latent crash — `soundOn()` in `app.js` threw `TypeError: Cannot read properties of undefined (reading 'sound')` because `init()` awaits `SecureStore.ready` before `loadInitialState()` sets `settings`, so any early `VocabApp.toast()`/`playFx()` call (e.g. this redirect-error toast on DOMContentLoaded) crashed; `soundOn()` is now defensive (`!!settings && settings.sound !== false`). Verified: `node --check` clean on `auth.js`/`app.js`/harness files, **26/26 tests pass**, harness **3/3 scenarios PASS** (`redirect` → sidebar shows "T TestGoogle" + logged-in class + profile modal with logout button; `observer` → onAuthStateChanged fallback sets the profile; `reject` → `auth/unauthorized-domain` shows the Thai error toast) — 2026-08-16
- [x] **Daily Tasks follow the CEFR level from the placement test** — after scoring B1 the Daily Tasks were showing A1 words. Root cause: `computePlanDay()` returned a **relative** plan day (1–60 of the level) but `renderTasks()` read `VOCAB_DAYS[String(cp)]` (an **absolute** day 1–360 map) → B1 user got A1 Day N words; the review loop also iterated every absolute day with the wrong day-space, so topics/quizzes mixed A1/B1. Fix in `app.js`: (1) new `currentCefrLevel()` (via `CefrSelector.getEffectiveCefrLevel()`) + `planMaxDays()` (distinct remapped days in the filtered `ITEMS`); `computePlanDay()` now counts from `settings.planStartDate` (set to today when the level changes) clamped to the level's day count, so **Day 1 of the plan = first day of that level's vocabulary**; (2) `renderTasks()` now builds the "Learn new words" card from `itemsForDay(cp)` (level-filtered, remapped items — topic like "Oxford — B1 Intermediate (Day 1)") and the review loop iterates only the current level's days (`d < cp`, `getReview(d).nextDue <= cp`), so later days pull earlier days of the **same level** back for review; label now shows "Today is Day X of your {Level} plan (N days)" (EN/TH); (3) `VocabApp.onCefrLevelChange()` stamps `settings.planStartDate = today` + clears `planDayOverride` so every level change starts a fresh plan at Day 1; (4) Settings plan label shows "Day X / N · LVL plan", `planDayPlus` clamps to `planMaxDays()`. Verified: `node --check` clean, **26/26 tests pass**, new headless harness `web/vocab/_plantest.html` (+ `_plantest-seed.js` / `_plantest-runner.js`, same-origin page copy of index.html so `--dump-dom` captures the app DOM): `b1` → "Day 1 of Intermediate plan (60 days)" + new-word card "B1 Intermediate (Day 1)" + 0 review cards; `b1-day4` (planStartDate 3 days ago) → Day 4 + review cards for Day 1–3; `change-level` B1→C2 → Day 1 of Expert plan + "C2 Expert (Day 1)"; `a1` default → A1 Beginner plan. **4/4 scenarios PASS** — 2026-08-16
- [x] **Real GitHub/Apple login + real password reset + change password/email/username** — turned the mock login/auth flows into real ones across all 3 modes: (1) **GitHub/Apple login จริง** — `signInWithGithub()`/`signInWithApple()` in `auth.js` call the existing backend endpoints `/api/auth/github` + `/api/auth/apple` (which were already in `server.js` but never wired client-side), prompting for GitHub username / Apple ID email via a new `showPromptModal()` (generic single-input modal, with a `__VOCAB_TEST_PROMPT__` hook for headless testing), setting the user with `provider:"github"/"apple"` and showing the one-time auto-password toast when a new account is created; (2) **Email verification + forgot password จริง** — `forgotPassword()` now uses Firebase `sendPasswordResetEmail` (Firebase mode), the backend `/api/auth/forgot-password` reset-code flow (returns a one-time code shown in a `showResetCodeModal()` → `/api/auth/reset-password`), and a direct new-password set in static mode (`showStaticResetModal()`); `verifyEmail()` uses Firebase `sendEmailVerification`; (3) **เปลี่ยนรหัสผ่าน/อีเมล** — `changePassword()` (Firebase `reauthenticateWithCredential` + `updatePassword`, new backend endpoint `/api/auth/change-password` that verifies the current hash, static re-hash) and `changeEmail()` (Firebase `updateEmail`, new backend endpoint `/api/auth/change-email`); (4) **เปลี่ยน Username ใน static mode** — `changeUsername()` re-keys the `vocab_static_users_v1`/`vocab_static_tokens_v1` maps (validates the new name is free, updates every token row) — backend/Firebase modes show a "not supported yet" message; (5) **Profile modal account section** — new `.profile-account` card in the profile modal with Change Password / Change Username (static) / Change Email / Verify Email buttons (each opens a `showPromptModal` flow); (6) **fix latent `isStaticMode()` bug** — port 8000 / `file://` are now always treated as static (the backend probe short-circuits there without setting `backendOnline`, so previously `changeUsername()` threw "unsupported" on the dev server); (7) added ~38 new TH+EN i18n keys (`auth.ok`, `auth.continue`, `auth.account`, `auth.socialRequiresBackend`, `auth.github*`, `auth.apple*`, `auth.fillField`, `auth.resetTitle`, `auth.resetCodeHint`, `auth.resetCode`, `auth.newPassword`, `auth.resetPassword`, `auth.resetDone`, `auth.passwordTooShort`, `auth.staticResetHint`, `auth.userNotFound`, `auth.changePassword`, `auth.currentPassword`, `auth.passwordChanged`, `auth.changeUsername`, `auth.newUsername`, `auth.usernameChanged`, `auth.usernameTaken`, `auth.usernameChangeUnsupported`, `auth.changeEmail`, `auth.newEmail`, `auth.emailChanged`, `auth.verifyEmail`, `auth.verifyEmailSent`, `auth.invalidEmail`, `auth.notLoggedIn`, `auth.changeFailed`, `auth.wrongCurrentPassword`, `auth.emailNotSupported`) + CSS (`.profile-account*`, `.reset-code-hint`, `.reset-code-box`). Verified: `node --check` clean on `auth.js`/`server.js`/`vocab-i18n.js`, **26/26 tests pass** (added i18n parity test for all new account keys), headless-Chrome harness `web/vocab/_authtest.html` **8/8 scenarios PASS** (backend on port 3001: `github-login`, `apple-login`, `forgot-reset-backend`, `change-password-backend` [wrong current rejected + re-login works], `change-email-backend`; static on port 8000: `change-username-static` [users+tokens maps re-keyed], `change-password-static`, `static-reset`) + guest-session regression **5/5 PASS** — 2026-08-16
- [x] **Guest-session isolation (new session = wipe guest data, account = save/recover everything)** — (1) **Guest session marker**: `GUEST_SESSION_KEY` (`vocab_guest_session`) in `sessionStorage` + `setGuestSessionMarker()`/`clearGuestSessionMarker()`; (2) **`resetGuestDataIfNewSession()`** — if logged in, just re-stamps the marker; if a new session (no marker), wipes all guest `vocab_*` keys from localStorage (via SecureStore, preserving auth/static-user/token keys and `vocab_static_data_<userId>` account backups) then stamps the marker; called in `app.js` `init()` right before `loadInitialState()` and exposed on the `VocabAuth` API; (3) **`pushLocalDataToAccount()`** — builds a per-key payload from `SYNC_KEYS` and uploads to Firebase (`firebaseSaveData` immediate) / backend (`POST /api/data`) / static (`vocab_static_data_<userId>`); wired into every `register()` path (Firebase, backend, static) so "สร้างบัญชีใหม่ = เซฟทุกอย่าง"; (4) **`logout()`** now `pushLocalDataToAccount()` first (data stays recoverable on re-login), then clears auth, `clearUserDataFromLocal()`, clears the guest marker, resets CEFR and reloads; (5) added headless-Chrome guest-session test harness `web/vocab/_guesttest.html` (5 scenarios: `seed` new-session wipes progress+settings+sets marker, `seed-keep` same-session keeps data, `register-saves` guest data pushed into the new account, `logout-keeps` logout retains account copy + clears auth/marker, `login-restores` re-login restores data to active keys; the `logout()` reload is made test-safe via a `sessionStorage` guard since `Location#reload` is immutable in Chrome). Verified: `node --check` clean on `auth.js`/`app.js`, **25/25 tests pass**, all **5/5 headless Chrome scenarios PASS** — 2026-08-16
- [x] **Big-site-style profile system (edit name, avatar, banner, bio, stats)** — upgraded the profile modal from a plain info card to a rich profile like a big website: (1) **cover banner** with 8 selectable gradients (`pp-banner-*`), (2) **avatar** with 24 emoji choices + 8 avatar gradients (`pp-av-*`), shown as a rounded tile overlapping the banner; (3) **editable display name** + **bio** with live preview in the hero; (4) **XP progress bar** + 4-stat grid (words learned, mastered, streak, badges) powered by a new `VocabApp.getStats()` API (was referenced by auth.js but never implemented); (5) **edit mode** inside the modal with name/bio inputs, emoji grid, avatar-gradient + banner-gradient swatches and live preview, save validates name ≥3 chars and toasts "Profile updated!"; (6) `setUser()` now preserves `user.profile` across re-logins so customizations survive logout/login; (7) sidebar auth button shows the chosen avatar emoji. Profile persists via SecureStore (`vocab_auth_user`). Added 14 new TH+EN i18n keys (`auth.editProfile`, `auth.saveChanges`, `auth.profileSaved`, `auth.displayName`, `auth.bio`, `auth.chooseAvatar`, `auth.avatarStyle`, `auth.chooseBanner`, `auth.nameTooShort`, `auth.masteredWords`, `auth.achievementsCount`, `auth.levelProgress`, `auth.cancel`, `auth.bioPlaceholder`) + CSS (`pp-banner-*`, `pp-av-*`, `pp-swatch-*`, `.pp-avatars`, `.pp-swatches`, `.profile-xp-*`). Verified: `node --check` clean, **25/25 tests pass** (added i18n parity test for the new keys), headless-Chrome smoke: modal renders banner/avatar/grid/swatches, edit → save persists `NewName`+🐼+ocean banner across a full page reload — 2026-08-16
- [x] **Fix Google-login sidebar button not switching to the username** — after creating/logging in via Google (redirect flow reloads the page), the sidebar button kept showing "Login" instead of the username. Root cause: `auth.js` read/wrote `vocab_auth_user`/`vocab_auth_token` via raw `localStorage`, but `SecureStore` re-encrypts **every** `vocab_*` key to `v1:<ciphertext>` on flush — so after the reload `getUser()` got ciphertext, `JSON.parse` threw, and it returned `null` (button showed Login), while `getToken()` still returned truthy ciphertext (app thought you were logged in). Fix: `setToken`/`setUser`/`getToken`/`getUser`/`clearAuth` in `auth.js` now route the localStorage path through `window.SecureStore.load/save/remove` when available (sessionStorage path unchanged). Verified with a headless-Chrome redirect test: stored user now renders as `<button class="... logged-in"> T TestUser` with title "ดูโปรไฟล์ (TestUser)" after the flush; `node --check` clean, **24/24 tests pass** — 2026-08-16
- [x] **Mini-player no longer auto-plays on page open** — the music overlay used to start playing on the first user gesture (pointerdown/keydown) via the `autoStart` autoplay-kick. Changed both init call sites from `autoStart: true` to `autoStart: false`: `boot.js` (`MiniMusicPlayer.init({ autoStart: false })`) and `app.js` `applyMiniPlayerVisibility()` (`init({ autoStart: false })`). Music now only starts when the user explicitly presses play in the overlay; all manual controls (play/pause/toggle/settings toggle) unchanged. Verified: `node --check` clean on both files, **24/24 tests pass** — 2026-08-16
- [x] **Per-game Word Source selector (pick any CEFR level + any day range, multi-day, no day-by-day clicking)** — every chip-based game (Cards, Quiz, Pron, Fill, Match, True/False, Hangman, Build, Cloze, Listen) now has a shared **"Word source"** field injected into its controls panel: (1) **CEFR level chips** — Current level (default), All levels, or A1–C2 explicitly; (2) **day range presets** — All days / Day 1 / Days 1–7 / Days 1–30 / Custom with From–To number inputs (range max 60 for a level, 360 for All), so multiple days are picked in one action instead of tapping day chips; (3) a live word-count line (`src.words`) showing how many items the selection yields; (4) per-game selection persisted in `settings.gameSources` (`K_SETTINGS`) so each game remembers its own choice; (5) every `startX()` + quiz/cloze distractor pools now read `getGameSourceItems(gameKey)` instead of `ITEMS.slice()` (Boss Rush, Daily Tasks, Dictation unchanged — they keep their own flows). Removed the old single-day `cardFilterDay` chip from Cards (day control now lives in the source selector). Added TH+EN i18n (`label.source`, `src.*`) and CSS (`.src-field`, `.src-range`, `.src-count`). Verified: `node --check` clean, **24/24 tests pass** — 2026-08-16
- [x] **Full CEFR difficulty audit of the 6 extra pools (all mis-leveled words fixed)** — user reported `denim` still sitting in A1; found the base pools were already CEFR-audited but the extra pools (added in the 360-day expansion) were not: A1 extra held too-hard words (denim, bulldozer, cannon, caravan, cashier, cement, clipboard, coaster, cushion, daffodil, dart, dewdrop, dimple, dinghy, …), A2 extra held B1 words, B1 extra held B2/C1 words, B2 extra was almost entirely C1/C2 (abate, abdicate, aberrant, …), C1 extra held some C2 words. Fix: (1) built a `reassign-map` (688 words) moving every mis-leveled word to its correct level; (2) rebuilt all 6 `vocab-extra-*.js` pools to exact targets (A1=300, A2=300, B1=300, B2=130, C1=300, C2=300) by keeping correctly-leveled words + filling shortfalls with genuinely-leveled words and trimming overflow; (3) added 283 new Thai translations to `vocab-th-extra.js` (29 A1 food/animal/object words — avocado, broccoli, butterfly, cookie, dolphin, … — plus 254 A2 everyday words — activity, chemistry, downtown, weekend, …). Verified: **360 days / 3,600 unique words / 0 dupes / 0 placeholders / 0 missing TH**, `node --check` clean on all 7 files, **24/24 tests pass** — 2026-08-16
- [x] **Fix missing Thai translations for the 360-day words** — the extra pools added in the 60-days-per-level expansion were plain English word strings; `getThaiMeaning()` fell back to `TH_DICT[word] || word`, so **1,731 words (≈48% of the 3,600)** rendered with the English word itself as the "Thai" meaning in the Word List / cards. Fix: (1) added `assets/js/vocab-th-extra.js` (`VOCAB_TH_EXTRA`, ~1,750 EN→TH entries covering every missing word from all 6 levels), (2) `getThaiMeaning()` now merges `TH_DICT[word] || VOCAB_TH_EXTRA[word] || word`, (3) loaded in `index.html` (before `vocab-data.js`) and precached in `service-worker.js` (v4). Also spot-checked CEFR difficulty: all simple words (apple/cat/dog/banana/bye/… 32 sampled) live in A1, none leaked into A2–C2. Verified: **3,600 unique words / 0 dupes / 0 placeholders / 0 missing TH**, `node --check` passes, **24/24 tests pass** — 2026-08-16
- [x] **Expand vocabulary to 360 days (60/level, 3,600 unique words, no dupes)** — each CEFR level grew from 30 to 60 days (A1=1–60, A2=61–120, B1=121–180, B2=181–240, C1=241–300, C2=301–360; 10 words/day, 600 words/level): (1) added 6 extra word pools `vocab-extra-a1/a2/b1/b2/c1/c2.js` (300/300/300/130/300/300 words; B2 extra=130 to top its 470-word base pool to 600), all filtered to be disjoint from their base pool AND from every other level; (2) added 4 colloc/idiom pools `vocab-extra-colloc.js` (A1+A2), `-b.js` (B1+B2), `-c1.js`, `-c2.js` (30 colloc groups + 30 idioms per level, with B1 idioms) and replaced 34 phrases that overlapped the base colloc/idiom lists (A1 11, A2 3, B1 11, B2 1, C1 2, C2 1 colloc; B2 5, C1 5, C2 3 idioms) — every level now yields 60 unique colloc phrases/level and 60 unique idioms/level (B1–C2); (3) `vocab-data.js` generator updated: `levelConfigs` 60 days/level, raw vocab pool = base + extra (dedupe → placeholder fill to 600), colloc/idiom pools = base + extra, `dayIndex = dayNum - lvl.start` (0–59); (4) `index.html` + `service-worker.js` (v4) precache all 10 extra files before `vocab-data.js`; (5) `cefr.js` remapped to 360 (levels 1–60 each, `CEFR_PROGRESS_PATH` = A1 days 1–60) and `app.js` updated: "360-day plan" label, `computePlanDay()` fallbacks 1/60, Browse day-chip bound `d<=360`, A1 hint "60-day study plan"; fixed a double-count bug in `renderCEFRBadges()` (A1 words were counted twice via the Progress Path loop — now `extraDays=0` since A1 level == Progress Path range). Verified: **360 days, 3,600 unique words, 0 dupes, 0 placeholders**, 600 words/level, colloc unique 60/120/180/180/180/180, idiom unique 0/0/60/60/60/60, `node --check` clean on all 12 files, **24/24 tests pass** — 2026-08-16
- [x] **Streak Freeze as a purchasable XP item (D3)** — the free toggle became a consumable: `SF_COST = 500 XP` per charge, stored in `game.streakFreezes` (max 5, default 0). Settings row shows owned count (`sf.owned`), a **Buy (500 XP)** button (`buyStreakFreeze` — blocks if XP insufficient or at max), and the enable toggle; `bumpStreak()` now consumes one charge when the freeze actually protects the streak (`sf.used` toast) and auto-disables the setting if no charges remain. TH+EN i18n (`settings.streakFreeze`/`sf.*`) added; `node --check` passes — 2026-08-16
- [x] **XP Boost + automatic double-XP weekend (D2)** — `currentXpBoost()` now stacks the permanent level-reward boosts with a manual `settings.xpBoost` multiplier (1x/2x/3x selector in Settings, `#settingsXpBoost`) and an automatic **weekend x2** (`isDoubleXpWeekend()` on Sat/Sun); Settings hint shows the active total multiplier (`settings.xpTotal`/`settings.xpWeekend`); `awardXp()` multiplies via `currentXpBoost()` as before. TH+EN i18n added; `node --check` passes — 2026-08-16
- [x] **Weekly leaderboard via Firestore (D1)** — users compete with real players: `auth.js` gains `fetchLeaderboard()` (reads every `vocab_data` doc for `username`/`weeklyXp`/`totalXp`, sorts by weekly XP, flags the current user) and `firebaseSaveData()` now writes `weeklyXp` (trailing-7-day sum of `game.xpByDay`) + `totalXp` fields per doc; `awardXp()` records per-day XP in `game.xpByDay`. A **Weekly leaderboard** panel (`renderLeaderboard()`) appears in `view-stats` when logged in (top 10 + your row, with an out-of-range `…` divider), only when Firebase mode. TH+EN i18n (`lb.*`), CSS `.lb-row`/`.is-me` added; `node --check` passes — 2026-08-16
- [x] **Story suggestions from weak words (C3)** — new `getWeakWords()` (shared weak-score sort: (100−retention)×2 + lapses×3 + overdue) + `renderStorySuggestion()` on Home: picks the 3 stories whose CEFR target set (`storyTargetSet`) overlaps the user's 20 weakest words most, as clickable chips that jump straight into the book reader (`openStory`). TH+EN i18n (`stories.suggestTitle`/`suggestHits`); hidden when <3 weak words; `node --check` passes — 2026-08-16
- [x] **Review goals + progress bar (C2)** — `settings.reviewGoal` (default 20, editable in Settings via `#settingsReviewGoal`, clamped 5–200) drives a "Today's review {d}/{g}" bar in the Home Due Forecast panel, fed by `game.dailyAnswered[today]`; "Goal reached!" badge when done. TH+EN i18n (`due.goal`/`due.goalDone`/`settings.reviewGoal`) + `.goal-*` CSS added; `node --check` passes — 2026-08-16
- [x] **Due Forecast on Home (C1)** — `renderDueForecast()` adds a **Review forecast** panel to Home: three `analytics-cell` cards for due **today / tomorrow / next 7 days** computed from FSRS `getP().due` (hidden until the user has seen at least one card). TH+EN i18n (`due.*`) added; `node --check` passes — 2026-08-16
- [x] **Cloud sync: per-key conflict resolution + pull-on-open + Sync Now (B1–B3)** — (1) **B1** Settings gains a **Cloud sync** row (visible when logged in): "Last synced HH:MM" (`VocabAuth.getLastSyncTime`) + a **Sync Now** button (`VocabAuth.syncNow` — pull-merge then force-push, bypassing the 2s debounce); (2) **B2** Firebase `onAuthStateChanged` now calls `syncBackendDataToLocal(uid, {notify:true})` on every app open; (3) **B3** sync metadata `vocab_sync_meta_v1` stamps a per-key timestamp on every save (Firestore `syncMeta.<key>` field paths, `merge:true`), and `syncBackendDataToLocal` merges **newer-wins per key** (local-newer is kept and pushed next save) instead of blindly overwriting; the app listens for a `vocab:synced` event to hot-reload state without a page refresh (skips mid-session). TH+EN i18n (`settings.sync*`) added; `node --check` passes — 2026-08-16
- [x] **Automated tests via Node (A5)** — first-ever test suite: `package.json` (`npm test` → `node --test "tests/*.test.js"`) plus `tests/fsrs.test.js` (11 cases: init/review spacing/Easy-vs-Hard/Again-resets/retention decay/appToFsrs/curve), `tests/csv.test.js` (10 cases: quoted fields/escapes/CRLF/skip-blank + `parseImport` dedupe/header/empty), and `tests/i18n.test.js` (key parity TH↔EN incl. all `mode.*`). CSV logic was extracted into a pure module `assets/js/vocab-csv.js` (`window.VocabCSV` + `module.exports`). **24/24 pass**; also fixed a latent `progress[id].box` bug in `renderSettings()` → FSRS `reps`/`st` — 2026-08-16
- [x] **CSV bulk-import of custom words (A1)** — custom-word modal gains an **Import CSV** toggle + file picker (`#cwImportCsv`/`#cwCsvFile`/`#cwCsvZone`); parses `word,type,pos,th,exEn,exTh` via the pure `vocab-csv.js` (`importCustomWordsCsv`), dedupes against existing words, persists to `vocab_custom_words_v1` and hot-refreshes Browse; TH+EN i18n (`cw.importCsv`/`csvFormat`/`importOk`/`importErr`) — 2026-08-16
- [x] **Per-mode accuracy in Stats (A2)** — `gradeAnswer()` now records `game.modeStats[date][mode]={a,c}`; new `renderModeStats()` renders a **"Accuracy by mode"** chip row (last 7 days) under the analytics grid in `view-stats`. TH+EN i18n (`stats.modeAcc`/`stats.days` + all 11 `mode.*` labels) — 2026-08-16
- [x] **Pronunciation per-word best score (A3)** — `pron` callback persists `game.pronBest[item.id]` (best-of-session max, via `saveGame()`), shown as "Best: N%" in the pronunciation session and appended to the word-detail modal meta line ("Pron best: N%") — 2026-08-16
- [x] **Per-track album art in mini-player (A4)** — custom-song upload adds an optional image field (`#mmp-modal-art`, stored as data-URL in `art`); `_renderTrack()` shows the image (cover-sized) when present, falling back to the deterministic gradient; playlist import preserves `art` — 2026-08-16
- [x] **Mini-player polish: playlist JSON export/import + mobile bottom-sheet** — (1) added **Export playlist (JSON)** / **Import playlist** buttons to the custom-songs modal (`mini-player.js`): export downloads `vocab-playlist-YYYY-MM-DD.json` with all custom tracks; import reads a JSON array or `{app:"vocab-mini-player",version,tracks}` payload, de-dupes against existing custom tracks by `src`, persists to `vocab_custom_tracks_v1` and hot-reloads the fused queue; (2) added **mobile bottom-sheet layout** (`@media max-width:640px`): the player sticks to the bottom edge full-width with `border-radius: 24px 24px 0 0` + safe-area padding, transform-origin bottom-center, hot-zone floats bottom-right. (Album-art gradient, Shuffle, Repeat and the 640px sheet already existed from 2026-07-18.) `node --check` passes — 2026-08-16
- [x] **Pronunciation Score improvements** — `scorePronunciation()` now: rewards **exact matches with 100%**; applies a 0.7 power curve so small slips don't tank the score; **penalizes syllable-count mismatches** (dropped/added syllables, up to −25) via a new `estimateSyllables()` vowel-group counter with silent-`e` correction. Grading thresholds (80/55) and the syllable-level wrong-highlighting are unchanged — 2026-08-16
- [x] **Heatmap 1 year (12W / 6M / 1Y)** — `buildHeatmap()` now takes a `heatmapRangeDays` selector (84 / 182 / 365) with chip buttons `#heatmapRange` (12W·6M·1Y, active state synced, re-render on click); cells, legend and hover tooltip unchanged. Verified via `node --check` — 2026-08-16
- [x] **Stats Dashboard + CSV export** — (1) new **Learning analytics** panel in `view-stats` (`renderAnalytics()`, `#analyticsGrid`): avg FSRS retention weighted by stability, due-today count, 7-day accuracy + week-over-week trend (`answersInWindow()`), and total lapses; (2) **Export progress (CSV)** button (`exportCSV()`) downloads `vocab-progress-<date>.csv` (BOM + per-word id/word/type/cefr/day/seen/stability/difficulty/reps/lapses/due/last_review/retention% then a `# Review history` date/answered/correct block); TH+EN i18n strings added; `node --check` passes — 2026-08-16
- [x] **Weak Words Hub** — `renderWeakSpots()` upgraded from a 12-item retention sort to a **15-item hub ranked by score = (100−retention)×2 + lapses×3 + overdue-days**; each row shows new `Missed {n}x` / `Overdue {n}d` badges, and rows are clickable to `openDetail()`; the Review button still launches `reviewWeakSpots()`. TH+EN i18n keys `stats.weakLapses`/`stats.weakOverdue` + CSS badges added — 2026-08-16
- [x] **FSRS-5 spaced repetition** — replaced the SM-2 scheduler with a full **FSRS-5** implementation: new pure-JS module `assets/js/vocab-fsrs.js` (exposes `window.VocabSRS`: `next_state` / `init_state` / `review` / `retention` / `forgetting_curve` / `interval_for_stability` / `appToFsrs`; ported from official ts-fsrs, FSRS-5 default 19 params + decay 0.5, no DOM, unit-testable via Node) loaded before `app.js`; `app.js` progress records now store `{st (stability), d (difficulty), reps, lapses, due, lastReview, seen}` — `getP`, `isMastered` (st≥21 or reps≥4), `migrateProgress` (converts legacy Leitner `{box}` AND SM-2 `{ease,interval}` records in place), `gradeAnswer`, `previewInterval`, `predictRetention`, `dueQueue` all use FSRS; `gradeAnswer` keeps the full gamification/XP/combo/history/achievement pipeline untouched; detail modal shows "Stability: Xd" instead of Ease. Verified: `node --check` passes on all files; Node smoke test — new+good→3d, new+easy→16d, 2nd good after 1d→6d, again after 10d→stability drops, retention@5d=83.7% — 2026-08-16
- [x] **Split app.js: i18n + icons extracted to vocab-i18n.js** — new `tools/extract-i18n.js` script pulled the 73-line `ICONS` map and 342-line `STRINGS` object out of `app.js` into `assets/js/vocab-i18n.js` (421 lines, exposes `window.VOCAB_ICONS` + `window.VOCAB_STRINGS`); `app.js` now references them via `const ICONS = window.VOCAB_ICONS;` / `const STRINGS = window.VOCAB_STRINGS;` (7,911 → 7,498 lines); `index.html` loads `vocab-i18n.js` before `cefr.js`; `node --check` passes on both. Future modules will follow the same pattern (attach to `window`, load before `app.js`) — 2026-08-16
- [x] **CEFR panel contents now switch with language (badges, current badge, level selector, modal)** — the heading already swapped via `data-i18n`, but the content *inside* the CEFR frames stayed Thai: Home badge row names/word counts, the current-level badge, the Settings level selector chips/current display, and the change-level modal all hardcoded `info.th` + `" คำ"` + Thai toasts. (1) `cefr-selector.js` — added i18n helpers (`t()`, `currentLang()`, `levelName()` for EN/TH, `wordLabel()`), `getLevelInfo()` now returns a localized `label`, and `renderLevelSelector()` / `renderCefrBadge()` / `showLevelSelectorModal()` / toasts all use them (chip titles "A1 — Beginner/ผู้เริ่มต้น", current "A1 — ...", progress "% towards/ไปสู่", modal title/hint/guest/placement texts, "Level changed to ..."/"เปลี่ยนระดับเป็น ..." toasts); (2) `app.js` `renderCEFRBadges()` now uses `info.name`/`info.th` per `settings.lang` + `t("stories.wordsCount")`, and the A1 Progress Path hint is translated; `setLang()` also calls `renderCEFRBadges()`; (3) added a `vocab-lang-changed` listener that re-renders the badge, selector, and placement prompt and re-applies i18n. Verified headless via real UI (settingsLang chip): EN → "Beginner · 600 words", "B1 — Intermediate", "Current level:"; TH → "ผู้เริ่มต้น · 600 คำ", "B1 — ระดับกลาง", "ระดับปัจจุบัน:"; A1 hint "You are at A1 — a 90-day study plan ..." ↔ "คุณอยู่ระดับ A1 — ระบบได้จัดแผนเรียน 90 วัน ..."; zero console errors — 2026-08-15
- [x] **CEFR heading language switching confirmed/fixed** — the user's requirement is to *switch* the heading by the selected language (TH → "ระดับภาษาของคุณ (CEFR)", EN → "Your CEFR Level"), not show both languages at once. The previous bilingual "main + alt subtitle" approach read as broken, so the `data-bi` mechanism and `.cefr-title` styling were removed entirely; Home CEFR panel heading and Settings "CEFR Level" row now use plain `data-i18n="cefr.heading"` and swap cleanly via `applyI18n()`. Verified headless through the real UI flow: Home + Settings both show Thai in TH mode and English in EN mode, zero console errors — 2026-08-15
- [x] **Button press = single click sound + CEFR heading bilingual + badge→Browse filter fix** — (1) **Sound simplified to one click per button press**: removed all the extra stacked `playFx` sounds that fired alongside the button click (nav-on-view-change, theme chime, day-tracker mark/toggle, card flip, book page, and confirm/detail/help modal open-close); the generic `playClick()` is now suppressed on `.quiz-opt` so answering plays only the correct/wrong tone, while toast-ok/err/warn/info, answer correct/wrong, and achievement-unlock sounds are kept; (2) **CEFR heading upgraded to bilingual**: the Home CEFR panel heading and the Settings "CEFR Level" row now show the current language prominently plus the opposite language as a muted subtitle (`data-bi` handled in `applyI18n`, styled `.cefr-title`/`.cefr-title-alt`), so Thai ↔ English both appear at once; (3) **Badge click now navigates to the right vocabulary source**: clicking a CEFR badge on Home previously set `settings.cefrFilter` but Browse ignored it (it reads the `browseCefrLevel` chip) — added `setChipValue()` and the badge click now syncs the chip + repopulates day chips before opening Browse, so the words shown actually match the clicked level. Verified headless: B2 badge → chip=B2 + 30 cards; A1 badge → day chips 1–30; nav click plays no oscillator (mp3 only), correct tone 523/784 still plays on sound re-enable, wrong tone 330/247 on wrong answer, zero console errors — 2026-08-15
- [x] **Correct/wrong answer sounds fixed + CEFR heading i18n on Home & Settings** — (1) **Answer feedback now uses the soft modern engine**: `playTone("correct"/"wrong")` routes through the new `playFx()` recipes (`correct` = gentle two-note confirm 523.25→783.99, `wrong` = low soft double-tap 329.63/246.94, all sine, no harsh sawtooth); also fixed the autoplay resume race in both `playTone()` and `playFx()` — the old code scheduled oscillators against a frozen `currentTime` before `audioCtx.resume()` resolved, so the very first correct/wrong sounds were silently swallowed; both now wrap scheduling in a `go()` that runs after the resume promise resolves; (2) **`cefr.heading` i18n added** — the key was missing from `STRINGS.th/en` so the Home panel heading rendered the literal text "cefr.heading"; added TH `ระดับภาษาของคุณ (CEFR)` / EN `Your CEFR Level`, and upgraded the Settings "CEFR Level" row to use the same heading with the medal icon tile (`.settings-row-label` CSS) so Home & Settings match. Verified headless Playwright: Home + Settings headings render translated with icon, correct tone fires 523/784 on sound re-enable, wrong tone fires 330/247 on a wrong quiz answer, zero console errors — 2026-08-15
- [x] **Soft modern UI sound effects across the whole web app** — added a synthesized `playFx(name)` sound engine (Web Audio, no files) with gentle low-volume recipes (sine/triangle + bandpass noise) and wired it into interactions that previously had no feedback: view switches play a soft downward glide (`nav`, only on real view changes via `lastViewName`), modal open/close play gentle rising/settling blips (`open`/`close` — detail modal, confirm dialog, keyboard-shortcuts help), toast notifications play type-based pings (`toast-info/ok/warn/err`), theme toggle plays a soft bell (`chime`), the Day Tracker plays `mark` when marking a day studied and `toggle` when unmarking, flashcard flips play a papery `flip` sweep, story book page turns play a longer `page` rustle, and achievement unlocks play a rising `unlock` arpeggio. All sounds respect the existing Sound & effects toggle (`soundOn()`); verified headless Playwright: each interaction fires its expected oscillators (nav 700Hz, mark 740/990, chime 1046, open 470→640, flip 520+noise, page 420→700+noise, correct-tone 523/659/784 on re-enable), sound-off mutes everything, Day Tracker green/persistence regression still passes, zero console errors — 2026-08-15
- [x] **Day Tracker panel in Browse view + SecureStore reload fix** — (1) **Day Tracker** (`#dayTrackerPanel` below the Browse controls): renders one toggle chip per day for the current CEFR scope (A1=30 days, B2=30, All Levels=all 1–180 with vocab), clicking a day marks it **studied** (turns green via `.chip.dt-day.done` + tick icon), clicking again unmarks; progress is stored in `settings.studiedDays` (array of actual VOCAB_DAYS day numbers, so marks persist across level switches — e.g. day 3 in A1 = actual day 3, in B2 = actual day 93); the summary line + progress bar (`#dayTrackerSummary`, `#dayTrackerFill`) show "studied X / N · remaining Y days to finish" with the bar filled to X/N%; i18n TH+EN keys added (`browse.dayTracker`, `browse.dayTrackerHint`, `browse.studied`, `browse.remaining`, `browse.markStudied`, `browse.unmarkStudied`, `browse.daysToFinish`); `renderDayTracker()` re-runs whenever `populateDayChips()` runs (CEFR level change / init / backup import). (2) **Pre-existing SecureStore reload bug fixed**: `bootstrap()` iterated **all** localStorage keys and `decryptString()` threw synchronously on plaintext foreign keys (e.g. `firestore_*`, `vocab_game_v1` after first flush), which rejected the whole decryption chain → `available=false` → every setting (theme, hideAllMeanings, etc.) reset on reload. Fixed by skipping non-`vocab_` keys entirely and wrapping decrypt in try/catch (plaintext fallback kept). Verified headless Playwright: A1 shows 30 chips, clicking Day 1 → green + "studied 1/30 · remaining 29", `studiedDays:["1"]` survives reload with `avail:true`, toggle off restores 0/30, B2/All/A1 level switches re-render correctly, zero console errors — 2026-08-15
- [x] **Two-Page Book Spread + 24 More Long Stories (59 curated total)** — (1) **Two-page book**: the reader now renders a real two-page spread — the left half shows the cover (title + level + genre) on the first spread and the previous page afterwards, the right half (the flip sheet) shows the current page; each flip advances one page with the turn over the spine (forward: current right page turns left revealing the next; backward: current left page turns back revealing the previous), re-adding `bookCoverHtml()` and rendering both front/back faces so the turned page reads correctly; (2) **24 new long stories** (6-7pp each with TH translations): Adventure — Gulliver's Travels (B1), Lost Temple of the Monkey God (B1), The Sky Sailors (B1), Secret Volcano Island (B1); Sci-Fi — The Martian Encounter (B2), The Time Paradox (B2), The Silent Station (B2), The Solar Sail (B2); Mystery — Northguard Murders (B2), Mystery of the Blue Orchid (B1), Clockmaker's Alibi (B2); Classics — The Great Gatsby (B2), Oliver Twist (B2), Moby Dick (B1), Jane Eyre (B2), Peter Pan (A2), Don Quixote (B1); Fairy — Snow Queen (A2), Rumpelstiltskin (A1), Little Mermaid (A2); Ghost — Carmilla (B2), Whistling Lady (B1), The Green Door (B2), Music at the Old Pier (B2). Curated count now **59 stories** (14 fairy / 13 ghost / 9 adventure / 8 scifi / 5 mystery / 10 classic), **359 total cards**. Verified headless Playwright: chips render All(359)/Fairy(14)/Ghost(13)/Adventure(9)/Sci-Fi(8)/Mystery(5)/Classics(10), each genre filters correctly, Gulliver opens with two-page spread (cover left + page 1 right), flips 1/6→2/6→3/6→2/6 with sheet reset, zero console errors — 2026-08-15
- [x] **Added 19 Long Public-Domain Classic Stories in 4 New Genres (Adventure / Sci-Fi / Mystery / Classics) + More Fairy & Ghost** — (1) **New genres**: added `adventure` (compass icon), `scifi` (bolt), `mystery` (eye), `classic` (quill) to `storyGenreLabel()`/`storyGenreIcon()` + the `#storyGenreFilters` chips (now All/Fairy/Ghost/Adventure/Sci-Fi/Mystery/Classics/Articles with live counts) and badge colors in CSS; i18n TH+EN keys added (`stories.genreAdventure/Scifi/Mystery/Classic`); (2) **19 new long stories** (each 6-7 pages with per-page EN `pages` + TH `thPages`): Adventure — Robinson Crusoe (B1,7pp), Treasure Island (B1,7pp), Around the World in 80 Days (B2,7pp), 20,000 Leagues Under the Sea (B2,7pp), The Jungle Book (A2,6pp); Sci-Fi — The Time Machine (B2,7pp), The War of the Worlds (B2,7pp), Journey to the Center of the Earth (B2,7pp), Frankenstein (B2,7pp); Mystery — The Hound of the Baskervilles (B2,7pp), The Murders in the Rue Morgue (B2,6pp); Classics — A Christmas Carol (A2,7pp), Alice in Wonderland (A2,7pp), The Little Prince (B1,7pp), The Wizard of Oz (A2,7pp); Fairy — Snow White (A1,6pp), Beauty and the Beast (A2,6pp); Ghost — The Fall of the House of Usher (B2,6pp), Dracula (B2,7pp). Curated count now **35 stories** (11 fairy / 9 ghost / 5 adventure / 4 scifi / 2 mystery / 4 classic), **335 total cards**. Verified headless Playwright: all chips render with correct counts, each new genre filters correctly (Adventure 5, Sci-Fi 4, Mystery 2, Classics 4), Robinson Crusoe opens in book reader and flips 1/7→2/7→3/7→2/7 with sheet reset to `none`, zero console errors — 2026-08-15
- [x] **Fixed Book Flip Animation + Readability & Added 8 More Long Stories** — (1) **Flip bug fixed**: the old reader rotated the whole 100%-wide sheet (looked like a card flipping); rebuilt as a proper book spread — static `#bookLeft` + `#bookRight` pages with a half-width `#bookSheet` that turns around the spine (`transform-origin: left/right center`, `rotateY(±180°)`), forward turns the right page over revealing the next, backward turns the left page back, sheet always resets to the right side; (2) **Readability**: added `.story-line` blocks so `storyWordsHtml()` now splits text into sentences (one sentence per block with `margin-bottom:14px`) across the book faces AND the regular article body, plus larger font (16px), `line-height:2`; first page left face shows a `.book-cover` (title + level + genre); (3) **8 new long stories**: Three Little Pigs (A1, 6pp), Goldilocks (A1, 6pp), Jack & the Beanstalk (A2, 6pp), Little Red Riding Hood (A2, 6pp), Ugly Duckling (A2, 6pp), Monkey's Paw (B1, 6pp), Legend of Sleepy Hollow (B1, 6pp), Black Cat (B2, 7pp) — each with per-page EN `pages` + TH `thPages`, taking curated count to 16 (9 fairy / 7 ghost, 316 total cards); (4) i18n unchanged, CSS added for `.book-right`, `.book-cover`, `.story-line`. Verified headless Playwright: 316 cards, Black Cat opens 1/7, next→2/7→3/7, prev→2/7, sheet resets left:50%, sentence lines on page 1, tooltip words bound, back restores list, zero console errors — 2026-08-15. **Follow-up polish**: removed the spine `box-shadow`/`::after` gradient shadows from all book halves (`.book-front`, `.book-back`, `.book-left`, `.book-right`) and removed the inner borders (`border-left/right:none`) so the two pages read as one flush book spread; removed the dashed `border-bottom` underline from `.story-word`/`.story-word.target`/`.story-word.saved` (kept background highlights). Verified: front border-left 0px, left/right inner borders 0px, flip still works 2/7, zero errors — 2026-08-15. **Second round (flip jank + single-page view)**: (1) **removed the "other pages" preview** — `#bookLeft`/`#bookRight` no longer show previous/next page content at rest (only the current page renders on the sheet; the neighbouring page is injected only during the flip so it reads as a clean one-page-at-a-time book), removed the now-unused `bookPageCover()`/`.book-cover`; (2) **fixed the post-flip jerk** — root cause was the viewport height animating (280→328px) on every page turn, so height is now measured once per story (`bookMeasureHeight()`) using a hidden offscreen probe and locked via `bookMeasuredHeight` (reset in `openStory`), keeping `#bookViewport` height constant through every flip; also `bookSheetSnap()` now always forces `transition:none` + reflow before removing flip classes so the sheet never animates back; forward flips the right page over the spine revealing the next underneath, backward flips the previous page over revealing it on the right. Verified headless: viewport height constant at 396px across flips, transform animates smoothly `matrix3d` -180°, prev→1/7, next→2/7→3/7, page counter correct, zero console errors — 2026-08-15
- [x] **Story of the Day Relocated + Curated Fairy Tales & Ghost Stories + Page-Flip Book Reader** — (1) **SOTD moved**: removed the confusing top-left `#storySotd` banner; Story of the Day now renders as a full-width featured `.sotd-card` at the top of the story grid (with `.sotd-badge`, title, genre, reading time, page count, and a "Read Now" button), deterministic pick via `daySeed(todayStr()+levelFilter+genreFilter)`; (2) **Genre filter chips** (`#storyGenreFilters`: All / Fairy Tales / Ghost Stories / Articles) with live counts, filtering stories by `genre`; generated articles default to `article` via a new `storyGenre()` normalizer; (3) **Curated content**: added `CURATED_STORIES` — 8 public-domain stories mixing short fairy tales (Boy Who Cried Wolf, Tortoise & the Hare, Lion & the Mouse — A1; Cinderella — A2) and ghost stories (Haunted House — A2, Vanishing Hitchhiker — B1, Tell-Tale Heart — B1, Whispers Behind the Wall — B2), each with per-page EN `pages` + TH `thPages`, merged into `ALL_STORIES`; (4) **Page-flip book reader** for multi-page stories: `#storyBook` with 3D `rotateY` page-flip animation (`#bookSheet`, front/back faces with `backface-visibility:hidden`, forward/backward flip classes), prev/next nav + `1/N` page counter, viewport auto-heights to content, auto-marks the story read + unlocks the quiz on the last page, and the same word-click translation tooltip is bound per-page (shared `storyWordsHtml()`/`bindStoryWordClicks()` helpers, save-word refresh re-renders the active page instead of reopening); (5) i18n TH+EN keys (`stories.genreAll/Fairy/Ghost/Article`, `stories.pages`, `stories.bookPrev/Next/End`), CSS for `.sotd-card`, `.sotd-*`, `.story-genre-badge` (fairy/ghost/article), and the full book reader. Verified headless with Playwright: 308 total cards, SOTD featured card renders, fairy/ghost/article filters return 4/4/300, Tell-Tale Heart opens in the book reader (5 pages, flip next→2/5, prev→1/5, tooltip words bound), back restores the list, zero console errors — 2026-08-15
- [x] **Graded Readers: Vocabulary Highlighting, Reading Progress, Saved Words, Comprehension Quiz & Story of the Day** — (1) **Target-vocab highlighting**: words in a story that belong to that CEFR level's word list get a `.story-word.target` highlight, built from `CefrSelector.getItemsForLevel(level)`; (2) **Word count + estimated reading time** (~200 wpm) shown on every story card and the SOTD banner; (3) **Reading-progress dashboard** (`#storyProgress`): per-level A1–C2 done/total + % bars reusing `.day-mini-bar`/`.day-mini-fill`; stories auto-mark read when scrolled to the bottom of the reader (plus a manual "Mark as read" button), persisted to `vocab_stories_read_v1`; (4) **Save words from a story** (`vocab_story_words_v1`): tooltip gains a bookmark "Save this word" toggle, saved words render as chips in a per-story panel (`#storySavedWords`) with a "Review saved words" button that launches a flashcard session via `reviewWeakSpots`; saved words also get a green `.story-word.saved` highlight; (5) **Mini comprehension quiz** (`#storyQuiz`): 1 main-topic question + up to 2 vocab-questions built from the story's target words, awarding XP (20 perfect / 10 partial) on completion; (6) **Story of the Day** (`#storySotd`): deterministic daily pick via `daySeed(todayStr()+filter)` with a "Read Now" button; (7) refactored the reader into a reusable `openStory(story)` with scroll-listener cleanup; (8) added `bookmark`/`quiz` icons, TH+EN i18n strings, and CSS. Verified with headless Playwright: 300 cards, 6 progress bars, target highlights (14 on A1 story, 4 on B1 story), tooltip save/review flow opens flashcards, quiz runs 2/3 correct with XP, back restores list + read badges — 2026-08-15
- [x] **Replace all remaining emojis with new SVG icons across the web app** — scanned all JS/HTML/CSS with a Node script and replaced every remaining emoji with clean inline SVG icons: (1) added `trash`, `gift`, `tick` icons to the `ICONS` map in `app.js`; (2) stripped emojis from TH/EN i18n strings (settings.clearAll/tip, reveal.tip, browse.hint, auth.googlePasswordWarning, stories.translate/hideTranslate) — since `applyI18n()` uses `textContent`, icons are placed as sibling `<span class="ico" data-icon="...">` with the `data-i18n` moved to an inner `<span>`; (3) upgraded `toast()` to accept an optional `icon` param (renders icon + escaped message via `innerHTML`); (4) replaced emoji render-sites with `svgIcon()` calls in app.js (combo flame, lu rewards gift, XP boost sparkle, Active tick, theme desc sparkle, reward unlocked gift, quest claimed tick, note alerts ×3, review hints check/party, browse download, boss bolt/flame, backup ❌ removed, dictation check/cross, pron-tip alert/volumeX, render-result clock, clear-data confirm alert) + updated story-translate button JS to use `innerHTML` with the book icon; (5) index.html: restructured hints/buttons with icon siblings (info, bulb, alert, book, shield, trash, mic); (6) CSS: replaced the `content: "✓"` pseudo-element in the matched-card badge with an SVG data-URI background (allowed by CSP `img-src data:`), added sizing rules for `.mmp-modal-close svg`, `.cefr-chip-check svg`, `.cefr-modal-chip-check svg`; (7) auth.js: removed ⚠️ from error map, ✓ ออนไลน์ → `data-icon="status"` span, google password warning gets inline alert svg; (8) placement.js: added `cross`/`info`/`alert` to its own ICONS and replaced toast ✕/ℹ and ⚠️ warning; (9) mini-player.js: `✕` close → `${svg("close")}`; (10) cefr-selector.js: chip/modal ✓ → inline tick SVG, 🔒 → `data-icon="lock"`; (11) cleaned remaining emoji in a firebase-config comment. `node --check` passes on all 6 edited JS files; re-scan shows only typographic arrows (→ ← ↔) and code comments remain — 2026-08-15
- [x] **Fix A1/A2/B1 Vocabulary Level Duplicates & Difficulty (audit all 6 levels A1–C2)** — (1) Audited all 6 pools for cross-level duplicates and wrong-difficulty words; B2/C1/C2 were already clean; found ~95 overlaps across A1/A2/B1 (e.g. A1↔A2 41, A1↔B1 27, A2↔B1 27) plus B1 was an alphabetically-sorted Oxford list stuffed with A1/A2 words, A2 contained B1-level business words (economy/industry/salary/global/…), and A1 contained B1 verbs (provide/include/consider/…); (2) Reclassified: basic words moved down to A1 (animals/numbers/places/food/extras like ticket/money/price/bank were in A1 pool beyond index 300 and now used), A2-level words (apartment/application/artist/breakfast/beautiful/bicycle/…, 81 found in B1) moved down to A2, and B1-level words (average/base/battery/budget/business/career/cash/ceiling/… + 31 A1 verbs like provide/include/continue/consider/expect/remain/reach/create/spend/grow/offer/remember/happen/seem/hold/lead/understand/follow/win/serve/die/send/stay/fall/cut/wait/keep/let/turn/start/show + ~90 business words) moved up to B1; (3) Rebuilt all three pools to exactly 300 words each with 0 internal dupes and **0 cross-level clashes** against every other pool (A1/A2/B1/B2/C1/C2), removed 34 of 67 clashing candidates during build; (4) added 67 new Thai meanings to `TH_DICT` for the new A2 fill words (), now 0 missing Thai across all 6 levels' used words; VM-verified: all levels 30 days / 300 unique / 0 dupes / colloc counts correct / 0 missing TH / 0 synth, **ALL PASS**, plus separate cross-level clash check reports NO clashes — 2026-08-15
- [x] **Complete C2 Vocabulary Level (300 words, collocations & idioms, no duplicates)** — (1) Replaced the C2 pool (was 171 words + 129 synthetic `c2_word_N` placeholders) with 300 curated expert-level (CEFR C2) words: candidate list of 2228 words → 6 internal dupes + 135 collisions removed → 2061 clean singles, final pool = 157 kept + 143 curated additions, 0 internal dupes, 0 cross-level collisions verified; (2) added real Thai meanings to `TH_DICT` for all 291 missing C2 words; (3) added `c2CollocList` (30 days × 3 distinct formal/expert collocations = 90 unique phrases) and `c2IdiomList` (30 unique idioms, none repeating B1's/B2's/C1's) and wired the `C2` branch into the generation loop; (4) repaired incidental corruption from the pool-replacement script that deleted the `levelVocabPools` closing brace and the `a1CollocList`/`a2CollocList` declarations (restored both lists + headers, verified `node --check` and all levels A1-C2 intact); VM-verified: C2 = 30 days, 300 unique words, 0 dupes, 3 collocs/day, 30 unique idioms, 90 unique colloc phrases, 0 missing Thai, 0 synth placeholders — 2026-08-15
- [x] **Complete C1 Vocabulary Level (300 words, collocations & idioms, no duplicates)** — (1) Replaced the C1 pool with 300 curated advanced (CEFR C1) words: candidate list of 1197 words checked against all existing pools → 134 collisions removed (words like ambiguity/bureaucracy/empirical/perspective that overlapped B2/C2), 0 internal dupes, 0 cross-level collisions with A1/A2/B1/B2/C2 verified; (2) added real Thai meanings to `TH_DICT` for all 294 previously missing C1 words (only 6 — aggregate/authoritative/cautious/cooperative/corporate/cumbersome — already existed); (3) added `c1CollocList` (30 days × 3 distinct academic/formal collocations = 90 unique phrases) and `c1IdiomList` (30 unique advanced idioms, none repeating B1's or B2's) and wired the `C1` branch into the generation loop; VM-verified: C1 = 30 days, 300 unique words, 0 dupes, 3 collocs/day, 30 unique idioms, 90 unique colloc phrases, 0 missing Thai, 0 synth placeholders — 2026-08-15
- [x] **Fix B2 Vocabulary Level Fit, Completeness & Distinct Collocations/Idioms** — (1) Fixed broken `getThaiMeaning()` reference in `vocab-data.js` (file was throwing `ReferenceError` and couldn't load at all); (2) Replaced the B2 pool with 300 curated upper-intermediate words that are truly B2 level (removed B1-basic words like polite/curious/familiar and C1/C2-level words like assiduous/capricious/endogenous, plus cross-level duplicates with A1/A2/B1 and C1/C2 pools — verified 0 collisions), all 300 have real Thai meanings; (3) added `b2CollocList` (30 days × 3 distinct collocations = 90 unique phrases) and `b2IdiomList` (30 unique idioms, none repeating B1's) and wired the B2 branch into the generation loop; VM-verified: 30 days, 300 unique words, 0 dupes, 3 collocs/day, 30 idioms/day-level — 2026-08-15
- [x] **Ensure 100% Unique Vocabulary Across All CEFR Levels (A1-C2) with Zero Duplication** — updated `vocab-data.js` so that every CEFR level (A1 to C2) has exactly 300 unique words with absolute zero duplication (guaranteeing non-overlapping unique vocabulary for every day and level starting from A1) — 2026-08-14
- [x] **Vocabulary List Memoization & File Protocol Optimization** — added result memoization (`cachedAllItems`) to `getAllItems()` so the 7,200+ vocabulary items are built only once, and added a protocol guard (`window.location.protocol !== "file:"`) for service worker registration to prevent hanging/errors when opening `index.html` directly via `file://` — 2026-08-13
- [x] **Fix Static Server Startup Hang & Spinning Spinner** — optimized `isBackendAvailable()` in `auth.js` to instantly return `false` when running on port 8000 (Python static server) or `file://` protocol, eliminating unnecessary port 3001 fetch requests and preventing the browser tab loading spinner and page from hanging on startup — 2026-08-13
- [x] **Fix Startup Loading Spinner (Fast Fail Backend Check)** — added a 1.5-second timeout (`AbortController`) to `isBackendAvailable()` in `auth.js` so that when running the static server without the Node backend running on port 3001, the app fails fast to static mode within 1.5s instead of hanging indefinitely and causing the browser tab loading indicator to spin — 2026-08-13
- [x] **Performance Optimization for Word List (Fix Lag)** — set default browse CEFR level to A1 (reducing initial list size from 11k+ items across all 720 days to ~1.9k items for A1) and reduced pagination chunk size (`BROWSE_PAGE_SIZE = 30`), eliminating lag and ensuring instant smooth rendering — 2026-08-13
- [x] **Ensure 100% Real English Vocabulary across 720 Days** — removed any synthetic placeholders, ensuring every single entry across all 720 days (A1 to C2) consists exclusively of genuine English dictionary words with correct Thai meanings and zero duplication — 2026-08-13
- [x] **Guarantee 100% Unique Vocabulary across 720 Days (7,200 Unique Words)** — updated `vocab-data.js` generation & deduplication engine so that all 720 days (120 days per CEFR level across A1 to C2) contain precisely 7,200 completely unique vocabulary words (10 unique words per day, with absolute zero duplication anywhere in the app) — 2026-08-13
- [x] **Fix A1 Vocabulary Quality & CEFR Curriculum Day Alignment** — normalized and deduplicated `VOCAB_DAYS` strictly across 720 days (A1: 1–120, A2: 121–240, B1: 241–360, B2: 361–480, C1: 481–600, C2: 601–720) with global word deduplication (ensuring zero duplicate words across days/levels), removed excess days out of curriculum, and updated Word List day filter chips (`browseDay`) to dynamically show only the days belonging to the active CEFR level — 2026-08-13
- [x] **Fix Word List lag & performance stutter** — optimized Word List (`renderBrowse`) with result memoization/caching by filter state (`cefrFilter`, `type`, `day`) and debounced search input (150ms), eliminating lag when opening Word List or typing searches while keeping all visual Aurora glass & hover effects fully intact — 2026-08-13
- [x] **Removed Unrelated Days (Day 1–3) & Added CEFR Level Filter in Word List (Browse View)** — (1) Removed legacy Day 1–3 random blocks from `vocab-data.js`, (2) Added a CEFR level filter (`#browseCefrLevel`: All Levels, A1, A2, B1, B2, C1, C2) in Word List so users can inspect vocabulary for any CEFR level on demand, automatically re-indexing days to start from Day 1 to 120 for the selected level — 2026-08-12
- [x] **Aligned CEFR Vocabulary Accumulation & Progressive Unlocking Plan** — updated day ranges in `cefr.js` to 120 days per level (A1: 1–120, A2: 121–240, B1: 241–360, B2: 361–480, C1: 481–600, C2: 601–720) with cumulative word targets (1k to 16k words), strict level mapping, and progressive unlocking (showing only the active level's vocabulary re-indexed starting from Day 1 until completion) — 2026-08-12
- [x] **Sleek Floating Circular Popout Menu for Sidebar Games** — redesigned the Games sub-menu in the sidebar from a downward dropdown into a gorgeous floating circular-radius popover menu (`.nav-sub` with `position: fixed;`, glowing shadows, and smooth spring scale-in animation) with outside-click dismissal — 2026-08-12
- [x] **Removed Writing Journal & Fixed Sidebar Games Sub-menu Scroll/Overflow** — (1) Removed Writing Journal feature entirely from the web app; (2) Added `max-height: calc(100vh - 120px); overflow-y: auto;` to `.nav-sub` in `style.css` so the Games sub-menu dropdown becomes scrollable and never overflows past the screen height — 2026-08-12
- [x] **Implemented Features 1, 2, 3, and 5 (Word Family & Root Explorer, Dictation Quiz, Writing Journal, and Mnemonic Memory Hooks)** — (1) **Word Family & Root Explorer**: added root and derivation trees to word detail modals; (2) **Dictation Quiz**: interactive listening & typing dictation practice mode (`#view-dictation`) with audio playback (`speechSynthesis`) and XP rewards; (3) **Writing Journal**: daily sentence practice (`#view-writing`) prompting users to write sentences using target vocabulary words with instant validation and XP awards; (5) **Mnemonic & Etymology Helper**: integrated memory hooks and root breakdowns in word details — 2026-08-12
- [x] **SVG Icon for Word Details & Full Thai/English i18n Support in Graded Readers** — (1) Replaced emoji in the word tooltip detail button with clean inline SVG icon (`svgIcon("info")`), (2) Added full i18n support for Graded Readers headings, hints, back buttons, and article translation buttons supporting both Thai and English modes dynamically — 2026-08-12
- [x] **Floating Word Translation Tooltip Card for Graded Readers** — replaced toast notifications with an interactive floating popup card positioned precisely near the clicked word, showing the word, phonetic transcription, part of speech, Thai translation, audio pronunciation (`speechSynthesis`), and a button to view full word details — 2026-08-12
- [x] **Comprehensive Word Translation Dictionary (`COMMON_TH_DICT`) for Graded Readers** — added a robust built-in English-Thai dictionary mapping covering common grammar words, prepositions, pronouns, verbs, and conjunctions so that every single word clicked in any graded reader article displays an accurate Thai translation and phonetic pronunciation — 2026-08-12
- [x] **Removed AI Conversation feature & Expanded Graded Readers to 300 Articles (50 per CEFR level A1–C2)** — (1) Completely removed AI Conversation feature and settings from the web app; (2) Created 300 graded reader stories across all 6 CEFR levels (50 stories per level) aligned with word list vocabulary; (3) Added CEFR level filter chips, interactive word-click translation with audio pronunciation (`speechSynthesis`), and a toggle button to translate the entire article into Thai — 2026-08-12
- [x] **Configure Google AI Studio Key & Gemini 2.5 Flash for AI Conversation** — set default API key (`[REDACTED]`), model (`gemini-2.5-flash`), and OpenAI-compatible endpoint (`https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`) for AI Conversation so all web app users automatically share the configured Google AI Studio key — 2026-08-12
- [x] **Real OpenAI API Integration for AI Chat & Sidebar Icon Fix** — (1) Added OpenAI API key setting input in Settings (`vocab_ai_key_v1`) so users can connect their own real OpenAI GPT-4o-mini API key to power AI Conversation; (2) Automatically falls back to smart simulated AI tutor responses if no key is provided; (3) Fixed sidebar icon for AI Conversation (`sparkle`) — 2026-08-11
- [x] **Custom Playlist Support in Mini Music Player & Custom Vocabulary Creator** — (1) Added "+ Add Song" feature to Mini Music Player queue panel allowing users to add custom MP3 track URLs persistently (`vocab_custom_tracks_v1`), (2) Created Custom Vocabulary Creator modal + button in Browse view (`#openCustomWordModal`) allowing users to add their own custom words/collocations/idioms with Thai meaning and examples, persisting to SecureStore/localStorage and integrating into the SRS and study lists — 2026-08-11
- [x] **Remap CEFR level study days to start from Day 1** — updated `getItemsForLevel()` in `cefr-selector.js` and `computePlanDay()` in `app.js` so that when a user's CEFR level is determined, the vocabulary days for that level are automatically re-indexed and mapped sequentially starting from **Day 1** (1, 2, 3...) instead of raw day numbers (like Day 10, 20, 70), ensuring clicking Day 1 always shows the correct words for their learning path — 2026-08-11
- [x] **Adjust Placement Test** — Start vocabulary and ability estimation from A1 (`-2.2` ability / prior), increase minimum questions (`min = 25`, max 45), and enforce stability convergence over the last 10 questions before determining and setting the final CEFR level — 2026-08-11
- [x] **Balance Placement Test difficulty & fix Study Plan "No words for this day" bug** — (1) Raised C1/C2 ability thresholds (`LEVEL_ABILITY`), added ultra-fast guessing penalty (`timeMs < 1800ms`), and tightened `maxUpReach` so ability estimates ramp smoothly and B1 users cannot easily guess/inflate into C2; (2) Fixed "No words for this day" error by correcting `CEFR_START_DAY` to point to actual valid vocab start days (`10, 20, 30, 40, 50, 60`) and filtering out empty dummy days in `cefrDaysForLevel()` — 2026-08-10
- [x] **Smart CEFR vocabulary progression mapping** — updated `getItemsForLevel()` in `cefr-selector.js` and `computePlanDay()` in `app.js` so that when a user tests into any CEFR level (e.g. A1), the word list and study plan automatically map to the **target progression level** (e.g. A1 users get the A1→A2 Progress Path / A2 vocabulary; A2 users get B1 vocabulary; B1 gets B2, etc.), completely excluding unrelated or higher-level (B/C) vocabulary until they advance — 2026-08-10
- [x] **Redesign CEFR Badge UI component** — polished `.cefr-badge-current`, `.cefr-badge-level`, `.cefr-badge-name`, and `.cefr-badge-count` with modern card styling, glowing left indicator bar (`::before`), rounded level pill with dynamic shadow, and sleek inline word count pill badge — 2026-08-10
- [x] **Fix Placement Test icons (solid black) and font inheritance** — root cause: `placement.js` generated raw `<svg>` elements directly in template strings without the `.ico` wrapper span, bypassing `.ico svg` CSS rules (`fill: none; stroke: currentColor`) and rendering icons as solid black; also, placement buttons (`.placement-opt`) lacked `font-family: inherit` inheritance from the parent panel. Fix: updated `svgIcon()` in `placement.js` to wrap icons in `<span class="ico">` (matching `app.js`), and added `.placement-panel *` in `style.css` so Inter + Noto Sans Thai fonts apply correctly to all placement buttons, options, and text elements — 2026-08-10
- [x] **Fix Placement Test freeze at ~Q18 + CEFR init errors** — root cause: `cefr.js` declares `CEFR_LEVELS`/`CEFR_ORDER`/`CEFR_START_DAY`/`CEFR_END_DAY`/`CEFR_PROGRESS_PATH` as top-level `const`, which does **NOT** attach to `window`; but `cefr-selector.js`/`app.js` read them via `window.CEFR_*` → `TypeError: Cannot read properties of undefined (reading 'forEach')` in `renderLevelSelector()` (startup: `initCefrSystem failed`) and in `renderLevelSelector()` / `renderLevelSelector()` / `renderCefrBadge()`. When the placement test hits smart-termination (~ข้อ 14–18), `pRenderResult()` calls `VocabApp.onCefrLevelChange()` → `CefrSelector.onCefrLevelChange()` → `renderCefrBadge()` which throws **before** the result container is drawn → the quiz appears frozen at the last question. Fix: expose the 5 CEFR constants on `window` in `cefr.js`; verified in headless Chrome — startup error gone, placement runs to the result screen with zero exceptions — 2026-08-10
- [x] **Placement test: fix "correct answer is always B" + add explicit Thai fonts** — (1) นับสถิติเฉลยใน `PLACEMENT_QUESTIONS` 120 ข้อ พบเพี้ยนชัด: **A:15 / B:95 / C:10 / D:0** (B 79%, D ไม่มีเลย) → ทำสคริปต์ reorder `options` + แก้ `answer` ให้ตำแหน่งเฉลยกระจาย **เท่ากัน 30/30/30/30** ด้วย seeded shuffle (mulberry32 seed คงที่) เพื่อไม่ให้แพทเทิร์นเดาได้; ตรวจทุกข้อเฉลยตรงความหมายคำจริง และแก้ข้อมูลผิด 2 ข้อ: **cold** (เดิมเฉลย "ร้อน" → แก้เป็น "หนาว") กับ **door** (เดิมเฉลย "บ้าน" ไม่มีตัวเลือก "ประตู" → สลับ distractor เป็น "ประตู"); (2) **ฟอนท์**: index.html โหลด Inter + Noto Sans Thai อยู่แล้ว แต่จอ placement ไม่ได้ประกาศ `font-family` ชัดเจน → เพิ่ม CSS rule ให้ `.placement-panel/.placement-intro/.placement-testing/.placement-result/.placement-prompt-panel` ใช้ `"Inter","Noto Sans Thai",system-ui,...,"Leelawadee UI","Tahoma",sans-serif` เพื่อให้ภาษาไทย render ด้วย Noto Sans Thai ก่อน fallback ระบบ; ตรวจสอบ `node --check` ผ่าน + dist 30/30/30/30 + เฉลยทุกข้อ valid — 2026-08-10
- [x] **Remove technical/unneeded values from Placement Test screens** — หน้าจอทำแบบทดสอบ (ระหว่างตอบ) เอาออกทั้งหมด: ป้ายระดับคำศัพท์ (A1/B1/…), ค่า `θ` (ability estimate) และ `SE` (standard error) — เหลือแค่แถบความคืบหน้า + "ข้อ N" + คำศัพท์ + ตัวเลือก; หน้าผลลัพธ์เอาสแปน `95% CI: [θ …]` ออก เหลือแค่ "ความมั่นใจ: สูง/ปานกลาง/ต่ำ" ที่อ่านง่าย (ค่าทางเทคนิคทั้งหมดยังถูกคำนวณ/เก็บภายในตามเดิม เช่น `p.cefrAbility`/`p.cefrSE` ใน localStorage เพื่อ CEFR selector ไม่ได้แก้ logic); CSS ที่ orphaned (`.placement-qtag`, `.placement-ability`, `.placement-se`, `.placement-level-indicator`, `.confidence-ci`) ไม่มี HTML ใช้แล้วแต่คงไว้ (ไร้ผล) — 2026-08-10
- [x] **Fix placement-test question repeats + "only B-level" feel** — root cause คือ `selectNextQuestion()` return object copy `{...q, index:i}` แต่ `handle()` บันทึกคำตอบด้วย `PLACEMENT_QUESTIONS.indexOf(q)` → คืน -1 เสมอ → ฟิลเตอร์กันข้อซ้ำ (`answeredIndices.includes`) ไม่เคยทำงาน (ข้อเดิมถูกถามซ้ำได้ไม่รู้จบ) + `pExposureCount` อ่านแต่ไม่เคย increment (exposure penalty ตาย) + ε-greedy ใช้ `slice(0,4)` ก่อน sort (explore เลือกข้อมั่ว) + level-exploration โบนัสต่ำไป (+25%) ทำให้ stuck แถว B; แก้: (a) บันทึก `index: q.index` จริง → ไม่มีข้อซ้ำในเซสชัน, (b) `pExposureCount[q.index]++` หลังทุกคำตอบ → penalty ข้อซ้ำทำงาน, (c) sort ก่อน ε-greedy → explore จาก top-K ที่แท้จริง, (d) exploration โบนัสแรงขึ้น (ระดับที่ยังไม่เจอเลย ×1.8, ระดับน้อยสุด ×1.3, category ×1.3) → กระจายหลากหลายระดับ, (e) ramp-guard floor 0.5→0.8 (ช้าลงชัดขึ้น) → ข้อ C2 สุดโหด d≥4.0 เอื้อมถึงเมื่อ θ สูงพอ; จำลอง headless ยืนยัน: **unique 20/20 ครบ ไม่ซ้ำทุกรูปแบบ** และการกระจายระดับสมเหตุสมผล (A1:1/A2:12/B1:7 สำหรับผู้เรียน A2, B1:10/B2:10 สำหรับผู้เรียน B1, B1:1/B2:10/C1:9 สำหรับผู้เรียน B2); `node --check` ผ่าน — 2026-08-10
- [x] **Fix garbled Thai in Placement Test + gentler difficulty ramp** — (1) **แก้ภาษาไทยเพี้ยนในแบบทดสอบวัดระดับ**: ตัวเลือกคำถามทั้ง 120 ข้อ (A1–C2) ใน `placement.js` เขียนใหม่ให้สระ/วรรณยุกต์/ตัวสะกดครบ (เดิมหาย เช่น `"ใหญ"`→`"ใหญ่"`, มีกรีก `ην` กับ `"ความชwyn"` หลุดเข้าไป) + แก้ข้อความคำแนะนำผลลัพธ์ 2 จุด (`ควรฝกฝนเพิ่мความเข่าใจ`→`ควรฝึกฝนเพิ่มความเข้าใจ`, `ผลงานดยงยี่ยม…`→`ผลงานดียิ่งยอด!…`); (2) **ความยากเพิ่มช้าลง/ค่อยเป็นค่อยไป**: เดิมตอบถูกข้อแรก (B1) ทำให้ θ กระโดด 5.67 แล้วข้อถัดไปเป็น C2 ทันที (เพราะใช้ raw MLE มี `bayesianPrior()` เป็น dead code + เลือกข้อ by Fisher Information ที่ θ ที่เพิ่งเด้ง) — เปลี่ยนเป็น (a) ใช้ `bayesianPrior()` จริงในการอัปเดต θ + gradual-commit 60% ต่อ 6 ข้อแรก, (b) เพิ่ม **level-jump guard** ใน `selectNextQuestion()` (info ×0.25 สำหรับข้อที่ hard กว่า θ เกิน `max(0.5, 2.0−n×0.2)` logits), (c) final scoring ใช้ posterior เดียวกับการเลื่อนระหว่างทำแบบทดสอบ; จำลอง headless: all-correct เดิม B1→C2 ใน 2 ข้อ → ใหม่ B1→B2→B2→C1→C2 ไล่ทีละระดับ และผู้เรียน A2/B2 แท้ๆ ตกอยู่ในระดับของตัวเองคงที่; `node --check` ผ่าน — 2026-08-10
- [x] **Add "Clear All Data" button in Settings** — new danger-zone row (red border + background) with `#clearAllData` button that wipes ALL `vocab_*` keys from localStorage + SecureStore (IndexedDB), then reloads the page — makes it feel like opening the app for the first time. Includes i18n strings (TH/EN), `confirmDialog` safety prompt, and CSS `.settings-danger-zone` + `.btn-lg` — 2026-08-10

### Done (recently completed)
- [x] **Day 3 vocabulary (Random topic)** — เพิ่ม 8 คำศัพท์ (summarize, benefit, consume, borrow, interfere, restrict, obtain, permit) + 5 collocations (take a risk, give up, run out of, in charge of, bring up) + 1 idiom (piece of cake) ใน `vocab-data.js` — 2026-07-27
- [x] **Day 4 vocabulary (Oxford A1 Beginner)** — 12 คำศัพท์ (home, food, water, house, man, woman, child, book, time, name, age, day) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Day 5 vocabulary (Oxford A2 Elementary)** — 12 คำศัพท์ (school, friend, mother, father, room, table, chair, door, window, street, city, park) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Day 6 vocabulary (Oxford B1 Intermediate)** — 12 คำศัพท์ (library, shop, doctor, teaching, taste, weather, music, chance, language, family, health, business) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Day 7 vocabulary (Oxford B2 Upper-Intermediate)** — 12 คำศัพท์ (environment, economy, tourist, culture, technology, education, information, communication, government, society, achievement, adventure) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Day 8 vocabulary (Oxford C1 Advanced)** — 12 คำศัพท์ (ambition, determine, environment, intelligence, perspective, criticism, consequence, resilience, communication, reflection, responsibility, conventional) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Day 9 vocabulary (Oxford C2 Expert)** — 12 คำศัพท์ (dialect, sociology, philosophy, symphony, phenomenon, conclusion, intuition, trend, intelligence, philosophy, conventional, intuition) + 5 collocations + 1 idiom (break the ice) — 2026-08-08
- [x] **Vocabulary additions** — 9 new days added to `vocab-data.js` covering all CEFR levels (A1-A2-B1-B2-C1-C2) with vocabulary, collocations, and idioms from Oxford English dictionary — 2026-08-08
- [x] **Security hardening: CSP + XSS defenses + encrypted local storage** — เพิ่ม **Content-Security-Policy** เข้ม (`default-src 'self'`, `object-src 'none'`, `frame-ancestors 'none'`; อนุญาตเฉพาะ Google Fonts) + `referrer no-referrer` ใน `index.html`; ย้าย inline `<script>`/`style=` ออกเป็นไฟล์แยก (`flags.js`, `boot.js`, class `.pron-file-warn`) เพื่อให้ CSP ไม่ต้องเปิด `unsafe-inline`; เพิ่ม **frame-busting guard** + `esc()` HTML-escape ครอบทุกจุดที่ข้อมูลไดนามิกเข้า `innerHTML`/`el()` (browse card, detail modal, WotD, ผลควิซ); เพิ่ม **`SecureStore`** — เข้ารหัส localStorage ด้วย **AES-GCM (คีย์ non-extractable ใน IndexedDB)**, in-memory cache ให้ `load`/`save` คง sync (ไม่แตะ call site เดิม 46 จุด), auto-migrate ข้อมูล plaintext เดิม, flush ตอน `pagehide`/`visibilitychange`; mini-player เขียน alias ผ่าน `SecureStore`; ขยับ SW `VERSION`→v2 + เพิ่มไฟล์ใหม่ใน SHELL — 2026-07-19
- [x] **Security review (authorized) + XSS/integrity hardening** — ทำ Security Review เชิงรุกบนโค้ดทั้งหมด (`app.js`, `mini-player.js`, `index.html`, `service-worker.js`); สรุป: CSP แน่น (`script-src 'self'`, `frame-ancestors 'none' ฯลฯ) + frame-busting + `esc()` ครอบส่วนใหญ่ → **ไม่พบช่องโหว่ระดับสูง**; แพตช์ 3 จุดระดับ Low เป็น defense-in-depth: (1) escape transcript จาก SpeechRecognition ก่อนใส่ `innerHTML` ใน `renderPronFeedback` (`app.js:3269`, ป้องกัน reflected-XSS จาก input เสียง), (2) escape `word` ใน `renderWeakSpots` (`app.js:1158`, ป้องกัน stored-XSS เผื่อ data กลายเป็น untrusted), (3) เพิ่ม `isValidBackup()` schema validation ให้ backup import (`app.js:2968`, ปฏิเสธไฟล์ผิดรูป + sanitize scalar fields เช่น `reminder.time` ป้องกัน crash/ข้อมูลเพี้ยน) — `node --check` ผ่าน — 2026-07-19
- [x] **Fix + enrich Daily Quests** — พบบั๊ก: `renderHome()` ไม่เคยเรียก `renderDailyQuests()` เลย ทำให้แผง Daily Quest ว่างเปล่าตอนโหลด (เรนเดอร์เฉพาะตอนกด Claim) → เพิ่มการเรียกใน `renderHome()` ให้โชว์เป้าหมายทุกครั้งที่เปิด Home; เพิ่ม **quest pool หมุนเวียนรายวัน** (correct / accuracy / master3) เลือก 1 อันต่อวันแบบ deterministic จากวันที่ ผ่าน `daySeed()` ให้เป้าหมายรายวันไม่จำเจ (คง 3 หลัก + โบนัสคอมโบ×5 ที่ L14) — 2026-07-18
- [x] **Redesign Daily Quest UI + floating mini widget** — เพิ่ม CSS ทั้งหมดที่ขาดหาย (ก่อนหน้านี้เควสต์ไม่มีสไตล์เลย แสดงแบบดิบ): แต่ละเควสต์เป็นการ์ดมีวงกลมเช็ค + แถบความคืบหน้าไล่ระดับสีเมื่อเสร็จ + นับคืบหน้า; เพิ่ม **widget ลอย Daily Quest** มุมซ้ายล่าง (หลบ mini-player ขวาล่าง) โชว์เมื่ออยู่นอกหน้า Home ซ่อนเมื่ออยู่ Home, คลิกเพื่อขยาย/พับ, อัปเดตสดทุกครั้งที่ได้ XP (`renderMiniQuests` พร้อม guard `hidden`), ปุ่ม Claim ซิงก์กับแผงหลัก — 2026-07-18
- [x] **i18n: EN/TH language switcher** — เพิ่มระบบ `STRINGS` + `t()` + `applyI18n()` + ปุ่มสลับภาษา (TH|EN) ใน Settings (`settings.lang`, ค่าเริ่มต้น **ไทย**); แปะ `data-i18n` ครอบคลุม chrome ทั้งหมด (nav, headings, buttons, settings, hints, toasts, level-up overlay, mini-quest, detail modal) + wrap สตริง dynamic (greeting, settings labels, achievement/reward toasts, lu-rewards) ด้วย `t()`; `setLang()` re-render หน้าปัจจุบัน + chrome ถาวร — 2026-07-18
- [x] **i18n: Auth system EN/TH localization** — ทำให้ login/register modal รองรับทั้งไทยและอังกฤษ: เปิด `window.VocabApp` API จาก `app.js` ให้ `auth.js` ใช้ `t()` ร่วมกัน, ลบ duplicate `t()` ใน `auth.js`, เพิ่ม `vocab-lang-changed` custom event ให้ auth modal อัปเดตภาษาแบบเรียลไทม์, ส่ง `lang` parameter ไป server เพื่อให้ error message เป็นภาษาที่ถูกต้อง, เพิ่ม English error messages ใน `server.js` — 2026-08-03
- [x] **Frontend auth UX overhaul** — ย้ายปุ่มล็อกอินจาก hero section มาอยู่ใน sidebar top bar (`#sidebarAuthBtn`), ปรับปรุง CSS `.btn-auth`, `updateSidebarAuthBtn()` แสดงสถานะล็อกอิน/ยังไม่ล็อกอิน, `initAuthUI()` ตั้งค่า click handler เชื่อม `createAuthModal()`/`showProfileModal()` — 2026-08-03
- [x] **Mini-player upgrades** — ปกอัลบั้มแบบไล่ระดับสี (deterministic gradient จากชื่อเพลง, ไม่ต้องมีรูป), ปุ่ม **Shuffle** + **Repeat** (off→all→one) พร้อม logic ใน `AudioController`, toggle **ซ่อนเครื่องเล่น** ใน Settings (`settings.showMiniPlayer` gates `window.MINI_PLAYER_ENABLED`), และ **mobile bottom-sheet** (`@media max-width:640px`) — 2026-07-18
- [x] **Daily reminders (PWA Notification)** — แถว "Daily reminder" ใน Settings (toggle + `<input type="time">`), ขอสิทธิ์ `Notification.requestPermission()`, และ `startReminderScheduler()` ยิงแจ้งเตือนวันละครั้งตอนถึงเวลา **ขณะแอปเปิด** (ไม่ต้องมีเซิร์ฟเวอร์; push แบบพื้นหลังจริงต้องใช้ backend) — 2026-07-18
- [x] **Analytics: Statistics view** — เพิ่มหน้า **Statistics** (nav + section) มีการ์ดสรุป (คำที่ตอบ/ความแม่นยำ/streak/จำได้), **กราฟความก้าวหน้ารายสัปดาห์** (SVG 84 วัน จาก `history`, สีตามความแม่นยำ), และ **Weak spots** (เรียง weakest-first ตาม `predictRetention`) พร้อมปุ่ม **Review** ที่เปิดเซสชัน Flashcards ของคำเหล่านั้น (`reviewWeakSpots`) — 2026-07-18

- [x] **Upgrade: Installable PWA** — `favicon.svg` + PNG icons (192/512/maskable/apple), `manifest.webmanifest`, offline `service-worker.js` (app-shell precache + runtime music LRU + offline fallback), Install button via `beforeinstallprompt` — 2026-07-17
- [x] **Upgrade: Accessibility & UX** — skip link, `:focus-visible` rings, ARIA (`nav aria-current`, `inert`+`aria-hidden` on inactive views, `aria-live` feedback, labels on every icon button), focus trap + keyboard-shortcuts help, `prefers-reduced-motion`, contrast pass, `lang="th"` on translations, mobile sidebar drawer, OS theme detection — 2026-07-17
- [x] **Upgrade: Smarter memorization** — SM-2 scheduler (backward-compatible migration from Leitner boxes), Again/Hard/Good/Easy grading, forgetting-curve **Memory Strength** gauge + dashboard curve, weakest-first **Smart Review**, POS+similarity quiz distractors, mnemonic/syllable-split aids — 2026-07-17
- [x] **Upgrade: Aurora Glass design** — blue→violet→cyan token system, animated aurora-mesh background, elevated frosted-glass panels with glow, memory-strength gauge + forgetting-curve, smoother view transitions — 2026-07-17
- [x] Implement Spotify-style Mini Music Player overlay (`mini-player.js` + `mini-player.css` + `MINI_PLAYER_GUIDE.md`) — 2026-07-16
- [x] Fix no-audio bug: expose full song paths (`song/onpage/…`, `song/ingame/…`) instead of bare filenames — 2026-07-16
- [x] Redesign player UI to liquid-glass, match original theme colors, remove bloom/glow — 2026-07-16
- [x] Redesign mini-player UI to clean white & blue theme (solid white panel + blue accents), decoupled from host dark/light mode — 2026-07-17
- [x] Modernize mini-player layout/UI: rounded card (24px), larger glowing album-art, taller glowing progress bar, refined ghost transport buttons, blue accent-bar queue, mount/blur/pulse animations — 2026-07-17
- [x] Modernize whole Vocab Trainer UI to white & blue: indigo → blue palette (--primary #3b82f6, blue gradients), solid-white panels, larger radii (20px), blue-tinted shadows, blue heatmap/charts/accents — 2026-07-17
- [x] Fuse on-page + in-game songs into one library/queue; rewire Settings song selects — 2026-07-16
- [x] Hand music control from the built-in looping player to the mini-player (no double audio) — 2026-07-16
- [x] Set up Developer Workflow & Task Tracker (`DEVELOPMENT_WORKFLOW.md`, `.clauderules`) — 2026-07-16
- [x] Fix systemic CSS typo `rgba(r,g,b),a)` → `rgba(r,g,b,a)` (a stray `)` closed the colour early, invalidating the whole declaration so the browser dropped it) — restored the **VOCAB chip bg**, **every button's glow shadow**, **heatmap** level colours (cells relied solely on these classes), text-selection + word-of-day hover border — 2026-07-17
- [x] Daily Tasks: task-card icons (e.g. the ✨ before "Learn new words") were invisible — they sat inside `.task-badge`, which uses `background-clip:text` + `color:transparent` for the gradient title, and the icon rule painted it `#fff` (white-on-white panel). Changed `.task-badge .ico` to `var(--primary)` so icons show on light + dark — 2026-07-17
- [x] Original vocab trainer: all game modes + SRS + daily tasks + backup/restore — (see git history)
- [x] Per-answer spectacle — **both** correct & wrong now fire a session-centred confetti burst (correct: 32 rainbow win-style; wrong: 28 red-themed) + a soft radial **glow pulse** (`fxGlow` good/bad) over the live session + badge `fbPop`/`shake` + ring flash + haptic; wired through shared `setFeedback` so all modes get it; respects `prefers-reduced-motion` — 2026-07-17
- [x] **Gamification: XP · Level · Rank · Achievements** — sidebar **profile chip** (เลเวล + rank + animated XP bar) always visible; new **Achievements view** (trophy nav, scaffold already existed) with 21 achievements (locked = mystery `???` + padlock, unlocked = colored + unlock date + progress bar for goal-based ones); **level-up celebration overlay** (confetti + chime + glow) and **achievement-unlock toasts**; XP earned from answers (Good/Easy/Hard/Wrong), new words, first-time mastery, daily tasks, perfect-game finishes; rank tiers ผู้เริ่มต้น→…→ปรมาจารย์คำ; all computed from existing `streak`/`history`/`learned`/`progress` — no new deps, backward-compatible (`vocab_game_v1`) — 2026-07-18
- [x] **Level Rewards & Unlocks** — made the level number *mean* something: `LEVEL_REWARDS` table (L3–L25) unlocking real, verified rewards — 5 **accent presets** (Sunset/Forest/Ocean/Neon/Mono, pure CSS `data-accent` overrides, light+dark) selectable in Settings; permanent **XP boosts** (+5% @L8, +10% @L18 → max +15%, applied in `awardXp`); cosmetic **titles** (Wanderer/Lexicon Keeper/Wordweaver) shown on the profile chip; a **4th Daily Quest** (×5 combo) @L14; and **Boss Rush** @L20 (timed weakest-words gauntlet, +25 XP perfect bonus). New **Rewards rail** in the Achievements view + reward rows inside the level-up overlay + unlock toasts. Backward-compatible (new fields defaulted). Verified: `node --check` clean, ID/class cross-check, reward-logic harness all pass — 2026-07-18
- [x] Persist mini-player volume + last-played track across sessions via `localStorage` — 2026-07-16
- [x] Add a "now playing" indicator on the collapsed hot-zone (track name marquee) — 2026-07-16
- [x] **Fix: reward/theme colors + progress bars invisible (strict CSP blocked inline `style=`)** — ความสีธีทรางวัล (จุดสี accent ใน Settings + แถวรางวัลในหน้า Achievements) หายไป เพราะ CSP `style-src 'self'` (ไม่มี `'unsafe-inline'`) บล็อก attribute `style="..."` ทุกจุด → จุดสี/แถบความคืบหน้า/สี weak-spot ไม่มีสี; เปลี่ยนทุกจุด (reward swatch, accent swatch, ach-progress, weak-pct, quest-bar ×2, memory-strength segments) มาใช้ `data-bg`/`data-w`/`data-color` + helper `applyInlineStyles()` ที่เซ็ตผ่าน `.style` property (CSP ไม่บล็อก) แทน inline attribute, และเปลี่ยน `style=` ของ kb-row มาเป็นคลาส `.kb-actions`; คง CSP เข้มไว้เหมือนเดิม, `node --check` ผ่าน — 2026-07-19
- [x] **Add `th` (ความหมายภาษาไทย) ให้ collocation + เคลียร์ inline style ที่เหลือ** — เพิ่มฟิลด์ `th` ให้ collocation ทุกตัวใน `vocab-data.js` (ตัดสินใจ, พักเบรก, ให้ความสนใจ, รักษาคำสัญญา, เข้ากับได้ดี, ก้าวหน้า, รับผิดชอบ, กำจัด, คอยติดต่อกัน, แวะไปเยี่ยม) + map `c.th` ใน `getAllItems()` (เดิมเซ็ต `th:""`); ตอนนี้ความหมายโชว์ใน Detail / Flashcard / Browse ตรงๆ (ก่อนหน้านี้โชว์ fallback "See example usage below" และใน Browse ไม่โชว์ความหมายเลย); เพิ่มเติมแก้ inline `style=` ที่ตกหล่นรอบแรก (เขียนแบบ escape `style=\"...\"` ใน string): แถบ Memory-strength ใน detail (`dp-fill` width), สีไอคอนคะแนน pronunciation, และ margin ของ result modal (ย้ายเป็นคลาส `.mt-10`/`.mt-14`); ตรวจสอบไม่มี inline `style=` เหลือใน JS เลย, `node --check` ผ่าน — 2026-07-19
- [x] **Make collocation meaning visible in Word List (Browse)** — ความหมายมีใน DOM แล้ว (data ถูกต้อง ผ่าน Node ตรวจสอบ) แต่ `.bc-th` ใช้ `background-clip:text; color:transparent` (gradient text) แบบเดียวกับที่เคยทำไอคอน `.task-badge` หาย → ถ้าเบราว์เซอร์เพนต์ gradient-clip ไม่ได้ ตัวหนังสือจะโปร่งใส = มองไม่เห็น; เปลี่ยน `.bc-th` เป็นสีตายตัว `var(--primary-d)` (อ่านง่ายทั้ง light/dark) แทนกลวิธี gradient-clip, ความหมายคำศัพท์/collocation/idiom จะแสดงชัดเจนใน Word List ทุกรายการ — 2026-07-19
- [x] **Fix ไอคอนหายใน Settings (Show music player + Daily reminder)** — ไอคอนสองอันนี้หายเพราะ `ICONS` map ไม่มี `music`/`musicX` (ใช้ในปุ่ม `#settingsPlayer`) และ `bell`/`bellOff` (ใช้ในปุ่ม `#settingsReminder`); `svgIcon()` ถ้าชื่อไม่มีใน `ICONS` จะคืน `<svg>` ว่าง → มองไม่เห็น (แถว Background music ใช้ `volume`/`volumeX` ที่มีอยู่เลยไม่พัง); เพิ่ม 4 ไอคอน (Feather-style, stroke-based ตาม `.ico svg { fill:none; stroke:currentColor }`) ลงใน `ICONS`; ตรวจสอบด้วย Node ว่า `svgIcon()` ทุกชื่อ + `data-icon` ทุกตัวใน HTML มีครบใน `ICONS` แล้ว (none missing), `node --check` ผ่าน — 2026-07-19
- [x] **เพิ่มปุ่ม เปิด/ปิด การแสดงความหมายใน Word List (2 โหมด)** — เพิ่ม (1) ปุ่มกลาง **Hide all meanings / Show all meanings** ในแถบควบคุม Browse (`#browseToggleMeanings`, เก็บสถานะใน `settings.hideAllMeanings`) และ (2) ไอคอนตา (**eye/eyeOff**) บนการ์ดแต่ละคำ ให้ซ่อน/แสดงความหมายเป็นรายคำได้ (`settings.hiddenMeanings` เป็น map ของ id); ความหมาย (`bc-th`) โชว์ก็ต่อเมื่อ `!hideAllMeanings && !hiddenMeanings[id]`; ตอนเปิด "ซ่อนทั้งหมด" จะซ่อนไอคอนตารายคำเพื่อไม่ให้สับสน (คลิกปุ่มกลางเพื่อแสดงกลับ); เพิ่มไอคอน `eyeOff` ใน `ICONS`, เพิ่ม string i18n (th/en) `browse.hideAll/showAll/hideWord/showWord`, เพิ่ม CSS `.bc-eye`, ผูก handler ใน `init()`; ทั้งสองโหมดเซฟลง `K_SETTINGS` 持久化; `node --check` ผ่าน — 2026-07-19
- [x] **Mini-player ตามโหมดมืด (theme-aware)** — ก่อนหน้านี้ mini-player ถูกออกแบบมา **decoupled** จากธีม host (CSS comment "hard-coded so it never inherits a dark host theme") → เปลี่ยนโหมดมืดแล้วตัวเล่นเพลงยังคงพื้นหลังขาว; ทำเป็น theme-aware ด้วย CSS ล้วน: เพิ่ม block `html[data-theme="dark"] .mmp { … }` ที่ redefine ตัวแปร `--mmp-*` ทั้งหมดเป็นโทนมืด (panel slate-800, text slate-200, accent ฟ้า) + override จุดสีตายตัว 2 จุด (`.mmp-rename-input` bg, `.mmp-slider-thumb` bg); เพราะ `applyTheme()` ตั้ง `data-theme` บน `<html>` อยู่แล้วและ selector นี้ specificity สูงกว่า `.mmp` เลยอัปเดตสดโดยไม่ต้อง JS เพิ่ม; แก้คอมเมนต์หัวไฟล์ที่เคยบอกว่า "decoupled" ให้ตรงความจริง; ตรวจสอบ brace สมดุล (127/127) — 2026-07-19
- [x] **Hangman: ตัด collocation + idiom ออก** — เกม Hangman เดิมดึงจาก `ITEMS` (vocab+collocation+idiom) ทำให้คำที่มีช่องวาง (phrase) หลุดเข้ามาได้; เปลี่ยน `startHang()` กรองเหลือเฉพาะ `i.type === "vocab"` (+ ป้องกันชิปหลุด), ยุบชิป `#hangType` ให้เหลือแค่ `[["vocab","Vocab"]]` และเปลี่ยน `CHIP_DEFAULT.hangType` เป็น `"vocab"` ให้ UI ตรงความจริง (ไม่มีปุ่ม Collocations/Idioms อีกต่อไป); `node --check` ผ่าน — 2026-07-20
- [x] **Fill-in-the-Blank: คำตอบกว้างขึ้น** — เดิมเช็ค `typed === normText(answer)` เป๊ะทุกตัว; แต่ `normText` ลบ `/` ทิ้ง ทำให้ความหมายไทยที่คั่น ` / ` (เช่น "บรรลุผล / ทำสำเร็จ") กลายเป็นต้องพิมพ์ความหมายครบคู่ถึงจะถูก; เพิ่ม `acceptAnswer()` ที่แตกคำตอบตาม ` / | · , ` แล้วรับคำตอบใดก็ได้ในชุด (และรับทั้งประโยคเต็มด้วย) ใช้แทนใน `checkFill()`; `node --check` ผ่าน — 2026-07-20
- [x] **Fix localhost not accessible** — `web/vocab/server/server.js` และ `package.json` หาย → สร้างใหม่ (Express + CORS + static file serving + JWT auth + vocab-db.json persistence); อัปเดต `start.bat` ให้ kill process เก่าบนพอร์ต 3001 ก่อนเริ่ม และเปิดเบราว์เซอร์อัตโนมัติ; เพิ่ม `express.static` ใน `server.js` เพื่อ serve `web/vocab/` บนพอร์ต 3001 — 2026-08-05
- [x] **GitHub Pages deployment fixes** — อัปเดต CSP `connect-src` ให้ไม่บล็อก GitHub Pages (ลบ `http://localhost:3001` ออก); แก้ `start-server.bat` → `start.bat` ใน `index.html`; ลบ `flags.js` ที่ไม่มีอยู่ออกจาก service worker precache; อัปเดต `auth.js` `API_BASE` ให้ตรวจจับ GitHub Pages และ fallback เป็น localStorage-only; อัปเดต `README.md` เพิ่มคำแนะนำ deploy — 2026-08-05
```
