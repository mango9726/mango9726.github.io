{
  "name": "vocab-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "Vocab Trainer web app (vanilla HTML/CSS/JS, no build step) — see DEVELOPMENT_WORKFLOW.md",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}